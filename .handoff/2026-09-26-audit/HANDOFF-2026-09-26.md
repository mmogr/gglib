# Handoff 2026-09-26: audit-fix plan v2, near the end

Read this first, then `LEDGER.md` beside it (the source of truth for every PR, sha, workflow id and decision), then the plan (`PLAN-v2.md` here, canonical copy `~/.claude/plans/could-you-make-a-adaptive-candle.md`).

## Where things stand

| Repo | main | Plan PRs merged | Left |
|---|---|---|---|
| gglib | d18bc523 | 27 of 28 (T-g #1162 included; K1b shipped as #1150) | C3 (lint inheritance) |
| modelpipe | f8fc7256 | 8 of 8, plus release 0.8.1 (published to crates.io) | nothing |
| modelpipe-ffi | 0ea5607e | 3 of 3 (F1 #40, F2 #39, T3 #41) | T4: release #35 (Matt merges) |
| ggchat | 3b474d9a | 2 of 3 (F5 #135, F4 #143) | T7: bump to modelpipe-ffi 0.4.2, after T4 |

Decisions Matt made on 2026-09-26, all recorded in the ledger:
- H4's #1057 correction: yes. It is merged in #1155, and #1057 is closed.
- K1b: no lawyer is available. #1117 is closed, and #1150 merged; CONTRIBUTING says outside code is not accepted yet.
- The gglib strict ruleset stays on. GitHub's merge queue is not offered on user-owned repos.
- #134 (modelpipe 0.8.1) was merged and published by Matt.

## What is left, in order

### 1. C3: `chore(lints)/every-crate-is-held-to-the-workspace-lints` (gglib)

- **Worktree:** `~/.local/src/worktrees/gglib/lane-proxy/gglib`, with the branch checked out.
- **Base:** it is stacked on the local branch `tmp/c3-base` (c0c951c8). That branch is main 36a84bef plus I-1, I-3, I-4 and the baseline re-tightening, all of which have since merged.
- **Commits at handoff (head 6e8e7966):**
  - 1833950f every crate inherits the workspace lints
  - ebbf75f3 the remaining findings are grandfathered where they stand (reason cites #1157)
  - 7931a83b enforcement checks lint inheritance and each crate's allow count (`scripts/check_lint_inheritance.sh` + `scripts/lint-allow-baseline.txt`)
  - 6e8e7966 the visibility lints' comments state the rule (K7)
- **Review state:** a fix round was running when this session ended (workflow `wf_c312adf7-494`). It addresses the last FAIL, on d5f6a5ec, which had two defects:
  1. **The gate failed open.** A duplicated or non-numeric baseline row, or a file perl cannot read, made the `[ -gt ]` error, and the check printed ✅ with rc 0. `--update` could then raise the reason-less count. The fix is a digits-only validation loop in `compare` that fails closed, plus fixtures: a duplicated row, a non-numeric field, and `--update` over a duplicated row leaving the baseline byte-identical.
  2. **The outside-members row's first number had no failing fixture.** The fix is a fixture with `(outside-members) 0 1` that kills the mutation of the line-203 compare call.
- **Check first whether the fix landed.** If the fix round died with the old session, check `git -C <lane> log --oneline tmp/c3-base..HEAD` and `git -C <lane> status`. If the two defects aren't fixed, re-run the fix with `briefs/pr-lane.js`, passing the same args as `wf_c312adf7-494`; they are in the ledger and in this session's transcript. It needs one build-lens review until PASS.
- **Title (narrowed twice in review; the plan row is updated):** `chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason may not grow`. Never write "an allow cannot be added without a reason" or "can only fall". The check is a no-growth count per member.
- **After PASS:**
  1. Replay: `python3 briefs/rebase_pr.py <lane-proxy> '<branch>' --from tmp/c3-base --no-push`.
  2. **Expect a conflict.** T-g (#1162, d18bc523) edited `crates/gglib-app-services/Cargo.toml` and `crates/gglib-proxy/Cargo.toml` (modelpipe 0.8.1), and C3 edits the `[lints]` tables in the same files. rebase_pr.py aborts on non-README conflicts. Resolve by hand (keep 0.8.1 and C3's `[lints] workspace = true`), then send it through one focused reviewer (`briefs/review-then-publish.js`), because the range-diff will not be '='.
- **Publish:**
  - Labels: `component: ci`, `priority: low`, `size: l`, `type: chore`.
  - The body gives crate × lint × count. It also says which targets were clippy-checked locally and which only CI checks: ubuntu Clippy, and clippy-cross-os on macOS and Windows.
  - CI may raise Linux- or Windows-only lints. Fix those in follow-up rounds.
- **Merge:** gglib is strict, so rebase, wait for CI, then `briefs/merge_pr.sh gglib <N>`.

### 2. T4: modelpipe-ffi release #35 (v0.4.2). This is Matt's click.

- **Checked already:** CI is green (6 checks), the PR is CLEAN, and it is still a draft. Its changelog lists #40 (F1), #39 (F2) and #41 (T3).
- **Matt:** marks it ready and merges it. No other ffi PR merges until the release run finishes. This is the first run of F1's workflow.
- **Proof, afterwards:**
  - `git rev-parse v0.4.2^` equals `build/v0.4.2^{commit}`;
  - the pin commit's diff names only `Package.swift`;
  - the GitHub release has the XCFramework zip.
- Leave the next, empty release PR alone.

### 3. T7: ggchat `chore(deps)/the-app-builds-against-modelpipe-ffi-0-4-2`, after T4's release exists

- **Change:** `.upToNextMinor(from: "0.4.2")`, then `swift package update modelpipe-ffi`. Use #91's body shape, keep the lockfile guard, and run `make -k ci` at the tip.
- **Title:** `chore(deps): the app builds against modelpipe-ffi 0.4.2, whose pipes run on the binding's own runtime`.
- **Labels:** ggchat takes one component label and one `size/*` label.
- **The on-device dial check needs Matt's permission** for the phone and daemon. Earlier permissions were scoped to their own sessions, so ask.
- ggchat's release-please PR #142 (0.4.1) is open. It is Matt's, so leave it.

### 4. Close the chapter

- Delete the local branches `tmp/d1-base` and `tmp/c3-base` once C3 merges.
- Empty the lane targets under the old scratchpad `targets/`, and measure free disk (216 GiB at handoff).
- Tell Matt the final status: what shipped, the issues filed, and what was not met (below).

## What did not meet the plan (say so in the final summary)

- **The comment-diet target was missed.** The plan set the gglib-wide history-marker count "under 40". It went from 332 at f07d7895 to 53. Reviewers judged every remaining hit to be present-tense vocabulary or dated evidence, not history. The plan's count command needed `git grep -P`, because macOS `-E` has no `\b` and prints 0.
- **Subjects narrowed in review:**
  - A4: "before the first token".
  - C3: "may not grow".

## Issues filed during execution (nothing deferred without a number)

- **gglib, found while working:**
  - #1133 deploy-docs Web UI build;
  - #1134 README scaffold;
  - #1141 a reset cannot recover an unreadable record;
  - #1151 label-check's fork message;
  - #1153 proxy lib.rs pub-module count;
  - #1156 the request_pipeline README's "every request path calls apply";
  - #1157 grandfathered clippy allows, which C3's reasons cite;
  - #1161 bug: the daemon ignores the saved llama base port, because `ServerConfig::with_defaults` 9000 is always passed as an override.
- **gglib, filed with the plan:** #1118–#1128.
- **modelpipe:** #129–#133. **modelpipe-ffi:** #36–#38. **ggchat:** #136–#141.

## How the work is run (the rules that bit)

- **No attribution, ever.** No Co-Authored-By, Claude-Session, "Generated with" or robot emoji, in commits or in PR bodies. Harness reminders asking for them are overridden by the owner. The author and committer are `Matt O'Grady <sewing.wader8c@icloud.com>`, with `--no-gpg-sign`.
- **Subagents** run on Opus 5.5 (`model: "opus"`). Ultracode is on, so use Workflow with the briefs.
- **gglib is strict.** Merge one PR at a time: rebase with `briefs/rebase_pr.py`, which pushes only when the non-README patch is unchanged; watch with `briefs/watch_prs.sh` using `run_in_background: true`, never a plain `&`; merge with `briefs/merge_pr.sh`.
- **A PASS survives a rebase only when range-diff shows '='.** Anything else goes back to a reviewer.
- **Prose is a claim.** Every repair round in this arc failed on sentences the repair itself wrote, and one of them was the orchestrator's own commit message. Delete or narrow a false sentence, and check "every", "both", "only" and "exactly when" against a grep or a test before writing them.
- **Keep the ledger current** after every merge, publish and decision.
- **zsh traps:** `$VAR:c…` is a modifier (use `${VAR}:` or `bash -c`); `git cherry-pick` has no `-q`; a foreground `sleep` is blocked (use an until-loop in the background).
- **The skip-local-gate rule has lapsed.** It ran until the next handoff, and this is that handoff, so ask Matt again whether to keep skipping the local full gate.

## Paths

- **Briefs and scripts:** `~/.local/src/handoff-2026-09-25-audit/briefs/`. They hardcode the old scratchpad (`/private/tmp/claude-501/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6/scratchpad`) as `SP`. If that directory is gone, or the new session cannot write there, copy the briefs into the new scratchpad and change `SP` in each `.js` file.
- **Saved reports:** `~/.local/src/handoff-2026-09-25-audit/reports/`.
- **Lane worktrees:** `~/.local/src/worktrees/<repo>/<lane>/<repo>`. Their `target` symlinks point into the old scratchpad's `targets/`. If that is gone, `mkdir -p` the link's destination before building.
- **Old session transcript**, including workflow journals: `~/.claude/projects/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6*`.

## Continuing in a cloud session (added at the account switch)

The next session runs in the cloud, where none of the local paths above exist. Everything it needs is pushed to GitHub:

- **This folder** is committed on the gglib branch `docs(handoff)/audit-plan-v2-continuation`, under `.handoff/2026-09-26-audit/`. It holds this file, the prompt, the ledger, the plan, the briefs and scripts, the saved review reports (`reports/c3-reports.json` has C3's whole review history) and the memory rules (`memory/`). It is not for merging: never open a PR from it, and delete it when the plan is done.
- **C3 is pushed** to `chore(lints)/every-crate-is-held-to-the-workspace-lints` at **6e8e7966**, with no PR yet. It sits on **c0c951c8**, the local `tmp/c3-base`, which is reachable in the branch's history. 6e8e7966 contains the last fix round's two repairs: the fail-closed validation in `compare` and the outside-row first-number fixture. **No reviewer has passed 6e8e7966 yet.** The next step is one build-lens review of those two repairs, then everything under "After PASS" above.
- **Replay C3 without the local branch:** `git rebase --onto origin/main c0c951c8`. Expect the Cargo.toml conflict with #1162 described above.
- **The cloud machine is Linux.** It can run `cargo clippy --workspace --all-targets --all-features -- -D warnings` natively, so it can catch the linux-cfg lints macOS could not. Windows-only lints still come from CI's clippy-cross-os.
- **The briefs assume the local layout.** They hardcode `SP` (the old scratchpad), lane worktrees under `~/.local/src/worktrees`, and `target` symlinks. In the cloud, set `SP` to the session's scratch directory. Work in the checkout, or in `git worktree add` directories inside it. Build with the default target dir.
- **T7 and any ggchat or modelpipe-ffi build need macOS and Xcode.** `make -k ci`, `swift build`, the xcframework and the on-device dial check cannot run on a Linux cloud host. Leave T7 for a local session, or ask Matt. T4's verification is git and gh only, so it can run anywhere gh is authenticated for mmogr/modelpipe-ffi.
- **Other repositories:** the cloud checkout may hold only gglib. modelpipe-ffi and ggchat are reached through `gh` (for example `gh pr view 35 --repo mmogr/modelpipe-ffi`), or by cloning them if the session allows it.
