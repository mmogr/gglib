{
 "wf_28d1f849-b9e": [
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "base": "tmp/c3-base (c0c951c87e28eae6f8f898e657d99543c3829172). Not modified and not pushed.",
   "head_sha": "be39258c62a7dbc33b1b1720656afa47985875de",
   "commits": [
    "ca7e3b81 chore(lints): every crate inherits the workspace lints",
    "a9bda20a chore(lints): the remaining findings are grandfathered where they stand",
    "12eac4ac chore(lints): enforcement checks lint inheritance and each crate's allow count",
    "be39258c chore(lints): the visibility lints' comments state the rule"
   ],
   "summary": "C3 is done in the worktree /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib as four commits, in the order the orchestrator set. The branch is 349 files, +2289/-1219. Nothing is pushed. The author is Matt O'Grady <sewing.wader8c@icloud.com>, there are no trailers, and both trailer greps print 0.\n\n**What each commit does**\n1. **ca7e3b81, the inheritance change and the zero-risk fixes.**\n   - The 8 crates that set their own lints (app-services, axum, cli, db, proxy, runtime, tauri, src-tauri) now say only `[lints]\\nworkspace = true`.\n   - The root Cargo.toml adds one workspace allow, `missing_const_for_fn = \"allow\"`, with a 3-line comment. Measured: this lint flags different functions on different targets.\n   - `#[allow(unsafe_code, reason = \"dlopen of gtk-layer-shell; the FFI is the module\")]` sits on `mod layer_shell;` in src-tauri/src/tray/mod.rs.\n   - `cargo clippy --fix` was run once per target, with every lint allowed except the chosen zero-risk list (listed below).\n   - 21 doc_markdown findings in crate READMEs were fixed by hand; `--fix` cannot reach them because rustdoc reads them from OUT_DIR.\n   - The ts-rs bindings are regenerated: 12 files, doc comments only.\n   - This commit builds on all 3 targets but still has warnings. That is intended.\n2. **a9bda20a, the grandfathered allows.** 489 findings are allowed through 212 attributes naming 318 lints.\n   - The attributes sit on 148 functions, 7 structs, 1 static, 1 `use`, and 55 `mod` items. A `mod` item carries the allows for a child file that is already at its size cap.\n   - The allows were placed by a syn-based locator and generator in the scratchpad, not by hand.\n   - No crate-level `#![allow]` was added anywhere, including the tests/*.rs crate roots.\n   - Five allows carry a specific reason instead of the #1157 text: the 3 Tauri event handlers, the `log_from_frontend` command, and the macOS/Windows tray `sync`.\n3. **12eac4ac, the script.** `scripts/check_lint_inheritance.sh` plus `scripts/lint-allow-baseline.txt`, which starts at 505 allowed lints across 18 members, 182 of them without a reason.\n   - It runs in `make enforce` after check_file_complexity.sh, and in the ci.yml enforcement job after the TypeScript ratchet. That job needs only bash, awk and perl.\n   - It is documented in scripts/README.md (quick-reference row, a section, and the CI table), and I added it to CONTRIBUTING's CI table.\n4. **be39258c, the K7 comments.** The comment on `unreachable_pub` goes from 36 lines to the rule plus one sentence, and the one on `redundant_pub_crate` from 9 lines to two sentences. Comment lines only.\n\n**Measurements for the PR body (step 1)**\nClippy was run with `--all-targets --all-features`. Counts are the distinct crate/lint/file:line:col findings seen on any of the three targets (aarch64-apple-darwin, x86_64-unknown-linux-gnu, x86_64-pc-windows-gnu). Total: 1413.\n\n| crate | total | mac | linux | win | zero-risk fixed | missing_const_for_fn | left after the fixes |\n|---|---|---|---|---|---|---|---|\n| app-services | 181 | 181 | 181 | 181 | 109 | 9 | 63 |\n| axum | 141 | 141 | 141 | 141 | 112 | 7 | 22 |\n| cli | 402 | 401 | 402 | 401 | 229 | 16 | 157 |\n| db | 62 | 62 | 62 | 62 | 40 | 8 | 14 |\n| proxy | 281 | 281 | 281 | 281 | 184 | 10 | 122 |\n| runtime | 293 | 278 | 285 | 259 | 178 | 23 | 94 |\n| tauri | 1 | 1 | 1 | 1 | 1 | 0 | 0 |\n| src-tauri | 52 | 44 | 42 | 40 | 23 | 4 | 17 |\n\n- The \"zero-risk fixed\" column includes 37 closures that were later put back and grandfathered (35 in proxy, 2 in runtime).\n- The src-tauri total includes 6 unsafe_code errors that only Linux raises.\n- The full crate \u00d7 lint \u00d7 count breakdown is in `scratchpad/c3/crate-lint-table.md`.\n\nZero-risk lints applied by `--fix`, with counts: doc_markdown 354 (333 by `--fix`, 21 by hand), uninlined_format_args 221, redundant_closure_for_method_calls 93, use_self 56, map_unwrap_or 43, duration_suboptimal_units 28, needless_raw_string_hashes 26, semicolon_if_nothing_returned 14, derive_partial_eq_without_eq 12, ignored_unit_patterns 10, if_not_else 5, explicit_iter_loop 3, implicit_clone 3, redundant_else 3, bool_to_int_with_if 2, unnecessary_trailing_comma 2, unnested_or_patterns 1.\n\nGrandfathered, with counts: format_push_string 44, redundant_closure_for_method_calls 37, option_if_let_else 36, too_many_lines 27, float_cmp 26, needless_pass_by_value 25, significant_drop_tightening 22, cast_possible_truncation 19, cast_precision_loss 16, single_match_else 15, match_same_arms 14, manual_let_else 14, items_after_statements 12, ref_option 12, unnecessary_wraps 12, struct_excessive_bools 10, and 42 smaller lints totalling 107.\n\n**Which targets I could and could not check**\n- **Native aarch64-apple-darwin:** all 18 members. This is the same target as CI's clippy-cross-os macos-14 leg.\n- **x86_64-unknown-linux-gnu:** all 18 members, cross-checked from macOS. C code was built with zig cc, and a fake pkg-config let the GTK/WebKit build scripts pass. This is check-only and nothing was linked. It covers the Linux-only paths, including src-tauri's layer_shell and tray.\n- **x86_64-pc-windows-gnu:** all 18 members, with zig cc and llvm-windres. I could not check **x86_64-pc-windows-msvc**, which is CI's target: ring, aws-lc-sys and libsqlite3-sys need MSVC/the Windows SDK. The repo has no `target_env` cfg, so gnu should see the same gglib code as msvc.\n- **ubuntu native:** not run.\n- CI's Clippy job and clippy-cross-os are the final check.",
   "commands_run": "All run at the tip (be39258c) on macOS.\n- `cargo fmt --all --check`: OK.\n- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: clean on native macOS, on `--target x86_64-unknown-linux-gnu` (zig cc, fake pkg-config) and on `--target x86_64-pc-windows-gnu` (zig cc, llvm-windres).\n- `./scripts/check_rust_complexity.sh`: passes without `--update`.\n- `./scripts/check_boundaries.sh`: all boundary checks pass.\n- `make bindings-check`: 199 types, the bindings match.\n- `./scripts/check_workflow_yaml.sh`: OK. Ruby's YAML parser also reads the new enforcement step.\n- `make enforce`: exit 0.\n- `./scripts/check_lint_inheritance.sh`: passes. `--self-test` passes. It also passes under /bin/bash 3.2.\n- `shellcheck scripts/check_lint_inheritance.sh`: clean.\n- `make doc-check` (RUSTDOCFLAGS=-D warnings): all 17 documented crates rebuilt, exit 0.\n- `cargo test --workspace --no-fail-fast`: 111 test results, 4113 passed, 0 failed, 70 ignored. No loopback or firewall failures.\n\nPer-commit checks:\n- Commit 1 was built on its own tree on all 3 targets. It compiles, with the expected 491 findings; fmt and the ratchet pass.\n- Commit 2's tree is byte-identical to the tree measured at zero findings on all 3 targets (`git diff` against the saved tree is empty).\n\nStep 1 measurement: `cargo clippy --workspace --all-targets --all-features --message-format=json` per target, reduced to unique crate/lint/file:line:col rows. The script is at `scratchpad/c3/extract.py`, which follows macro expansions back to the workspace file.",
   "mutations": "Every mutation was made on a copy or reverted by rewriting the bytes, then re-run clean.\n\n**Script self-test (`--self-test` on a mutated copy): all 7 fail, exit 1.**\n- M1, cfg_attr allows ignored: \"an allow above the baseline passed\".\n- M2, `[lints.x]` tables not flagged: the first fixture did not fail on exactly the two bad manifests.\n- M3, growth in the count without a reason allowed: \"--update raised the count of allows without a reason\".\n- M4, growth in the total allowed: \"an allow above the baseline passed\".\n- M5, `reason =` counted as a lint: the first fixture fails.\n- M6, whole-line comments counted: the first fixture fails.\n- M7, extra keys under `[lints]` allowed: the first fixture fails.\n- The unmutated script passes.\n\n**Real tree (crates/gglib-db): each of these fails the script, and the restored tree passes again.**\n- `[lints.rust]` appended: \"declares [lints.rust]\".\n- `workspace = false`: \"[lints] holds `workspace = false`\".\n- `[lints]` removed: \"has 0 [lints] tables, not one\".\n- `lints = {\u2026}` written inline: rejected.\n- `#[allow(clippy::nothing)]` added: \"1 lints allowed without a reason, baseline 0\".\n- The same allow with a reason: \"10 lints allowed, baseline 9\".\n\nThe Rust changes add no behaviour and no tests. Their check is clippy being clean on 3 targets, plus the full test suite passing.",
   "deviations": "1. **The script counts more than `allow(clippy::`.** It counts every lint named in an `allow(...)` or `expect(...)` attribute, rustc lints included, and `cfg_attr` forms too. It also keeps a second number per crate, the lints without a `reason =`, which cannot grow and which `--update` refuses to raise. I added this because, with the plan's single count, the PR subject (\"an allow cannot be added without a reason\") would be false: a bare allow plus a baseline bump would pass. It is still a baseline file, so a hand edit of the third column gets around it. If you would rather keep the plan's exact scope, change the subject instead.\n2. **doc_markdown counted as zero-risk.** Its fix is machine-applicable and only touches docs, and 354 allows would have been unmanageable. It backticks words like `HuggingFace` and `OpenAI` in 20+ module READMEs (the convention the already-inheriting crates follow), and that forced regenerating 12 ts-rs binding files in commit 1.\n3. **Files at their size cap.** rustfmt breaks any allow with a reason across 3+N lines (its limit for attribute width is 70), so:\n   - The allows for 64 child files went onto 55 `mod` items. Some moved up two levels; for example forward_tests.rs goes to forward.rs, then to lib.rs's `mod forward;`.\n   - In 5 capped files (4 in proxy, runtime's system/mod.rs), 37 poison-recovery closures were put back, because the fix's path makes each chain wrap onto 3 more lines.\n   - In 4 capped files (mapper.rs; proxy's dashboard.rs, slots_poller.rs and token_calibration.rs), I shortened the paths the fix wrote through an import.\n4. **`setup_app` in src-tauri/src/main.rs is fixed instead of allowed.** main.rs is a crate root at its 303-line cap with no parent to carry the allow. It went from `&mut tauri::App -> Result<(), Box<dyn Error>>` to `&tauri::App` returning nothing; the call site changed from `setup_app(app)?;` to `setup_app(app);`. This is in commit 2.\n5. **Framework shapes allowed per item, not crate-wide.** No crate-root allows at all. The five Tauri-imposed shapes carry their own reason text at item level.\n6. **CONTRIBUTING's CI enforcement row** also names the new script; the orchestrator asked only for scripts/README.md.\n7. **Windows was checked as windows-gnu, not msvc**, and Linux by cross-compiling from macOS. See the summary for what could not be checked.",
   "review_focus": "- **The 55 module-level allows** hide future findings of those lints across whole modules. Examples: proxy lib.rs `mod forward;` allows 7 lints including cast_*, float_cmp and too_many_lines; `mod sampling_audit` and `mod token_calibration` allow significant_drop_tightening. The file caps forced this. It is the most visible trade-off.\n- **The `--fix` diff in commit 1** is 250 files. It deserves spot-checks of:\n  - if_not_else branch swaps, e.g. runtime/process/health.rs;\n  - map_unwrap_or rewrites to map_or or is_some_and;\n  - duration_suboptimal_units, e.g. `ADMISSION_DEADLINE` from `from_secs(180)` to `from_mins(3)`;\n  - 12 added `Eq` derives;\n  - implicit_clone, e.g. `to_path_buf()` to `clone()` in residency/launch.rs.\n  - Lock code only saw PoisonError closure rewrites. No significant_drop_* or cast_* finding was rewritten anywhere.\n- **The script's perl regex parser** (balanced parentheses, strings blanked):\n  - An attribute written inside a string literal is counted too. The header says so; none exist today.\n  - The inline `lints = { workspace = true }` spelling is rejected by design.\n  - `[workspace.lints.*]` allows in the root manifest are not counted.\n- **The PR subject versus the mechanism:** see deviation 1.\n- **The five special reason strings and the new `missing_const_for_fn` comment in Cargo.toml** are claims; I measured them, but they are worth a check.\n- **Commit messages give counts** (1413, 876, 491/489, 212/318, 505/182). The raw data is in `scratchpad/c3` (union1.tsv, m5-union.tsv, crate-lint-table.md). The scratch tools there are locate/ (the syn locator), gen.py, fix.sh, measure.sh and the zig wrappers."
  },
  {
   "verdict": "FAIL",
   "defects": [
    {
     "where": "crates/gglib-runtime/src/process/mod.rs:22-29 (`pub mod residency;`), crates/gglib-runtime/src/proxy/mod.rs:6-11 (`pub mod supervisor;`), crates/gglib-proxy/src/lib.rs:96-103 (`mod sampling_audit`) and :135-142 (`mod token_calibration`), crates/gglib-runtime/src/process/admission/mod.rs:3-8 (`mod state;`); also crates/gglib-proxy/src/loop_guard_step_trips_tests.rs:69/90/132 and crates/gglib-proxy/tests/fixtures/mod.rs:4",
     "what": "Every allow that hides significant_drop_tightening in residency and proxy lock code, and cast_possible_truncation in admission, carries only the generic reason \"grandfathered at lint inheritance, #1157\". The orchestrator's check requires a reason that says why for these areas, and these reasons do not say why. #1157's body reads \"It closes when the baseline file holds no grandfathered allows\", so it tells a future contributor to pay these down by tightening the drops. The plan says never to apply that lint in admission/residency or proxy lock code. When I removed the allows, these findings appeared: significant_drop_tightening at residency/mod.rs:610, residency/launch.rs:174 and :285, supervisor.rs:312, :463 and :512, sampling_audit.rs:770 and token_calibration.rs:367; cast_possible_truncation at admission/state.rs:737 (`as_millis() as u64`). Each of these allows sits on a `mod` item, so it also hides future findings anywhere under residency, supervisor, sampling_audit and token_calibration.",
     "fix": "In each of these attributes, move significant_drop_tightening (and significant_drop_in_scrutinee where present) into its own `#[allow(...)]`. Its reason should state the rule: the guard's scope is deliberate and the lint is never applied in lock code, not \"grandfathered\". Do the same for cast_possible_truncation on admission's `mod state`, with a reason saying why the millisecond truncation is acceptable. The parent files have room: process/mod.rs 45 lines, proxy/mod.rs 17, admission/mod.rs 28, proxy lib.rs 160, none in the ratchet baseline. Splitting leaves both baseline numbers unchanged; re-run `--update` to confirm. Edit #1157's body to exclude lock-code significant_drop allows from the paydown. Also correct its sentence \"counts the `allow(clippy::` occurrences\", which no longer describes the script."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:175-250 (selftest) and :107 (find)",
     "what": "The self-test does not guard the claim that tests/ crate roots are counted, which is in 12eac4ac's message and in plan decision 2. Its only counted fixture is good/src/lib.rs. I mutated line 107 to `find \"$1/src\"`, so that tests/*.rs, build.rs and benches are no longer counted. `--self-test` passed and the real check passed, because the counts fell below the baseline. With that mutation, a new bare `#![allow(clippy::pedantic)]` at the top of crates/gglib-integration-tests/tests/*.rs also passed. The header says a detector that stops detecting fails the self-test; for this dimension it does not.",
     "fix": "Add a fixture outside src/, e.g. `good/tests/it.rs` holding `#![allow(clippy::zeta)]` and a reasoned allow, plus optionally `good/build.rs`. Raise the expected fixture counts in the baseline lines and the grep messages to match. Re-run the `find \"$1/src\"` mutation and confirm the self-test now fails."
    },
    {
     "where": "crates/build_common.rs (outside every member directory); scripts/check_lint_inheritance.sh:104-114 (count_member only walks member dirs)",
     "what": "crates/build_common.rs is pulled into member build scripts with `include!(\"../build_common.rs\")`: agent, app-services, axum, bootstrap, build-info and cli, at least. It sits in no member directory, so no count includes it. A bare `#[allow(clippy::too_many_lines, clippy::pedantic)]` added there passes the script (rc=0), which makes the PR subject \"an allow cannot be added without a reason\" false for this file.",
     "fix": "Count every tracked .rs file that no member directory contains, or fail when such a file exists. For example, add a pseudo-row (e.g. `(outside-members) 0 0`) to the baseline, or have `check` scan `crates/*.rs` and name the file. Add a self-test fixture with a .rs file outside the members that must be counted or flagged."
    },
    {
     "where": "crates/gglib-app-services/src/remote/gateway_session.rs:103 and :172, remote/serve.rs:97, remote/serve_switch.rs:230, remote/stored_pairing.rs:68 (covered by lib.rs:35-41 `mod remote`); crates/gglib-app-services/src/benchmark/tune/scoring.rs:262-267 (covered by benchmark/mod.rs:34-42 `mod tune`)",
     "what": "Six item-level attributes, naming 8 lints, sit inside a subtree whose ancestor `mod` allow already names the same lints, so they have no effect. I removed the `mod tune` and `mod scoring_tests` allows and confirmed the scoring_tests findings come back. With only `mod scoring_tests`'s allow removed, clippy stays silent. These attributes inflate the no-growth baseline by 8. They also mislead a paydown: deleting one of them does not make clippy report the finding it appears to mark.",
     "fix": "Delete the redundant inner attributes and lower the baseline with `--update`. Alternatively, keep them and give each a reason saying the ancestor allow is what takes effect."
    },
    {
     "where": "a9bda20a commit message (\"on 148 functions\"); scripts/check_lint_inheritance.sh:31",
     "what": "Two nits. First, the 148 function attributes sit on 147 functions, because log_from_frontend carries two attributes, so \"212 attributes ... on 148 functions\" overcounts by one. Second, the header says \"Exit codes: 0 pass, 1 a rule failed or the self-test did\", but an unknown argument exits 2.",
     "fix": "Reword to \"148 attributes on 147 functions\", or equivalent, in the message and the PR body. Add \"2 usage\" to the exit-code line."
    }
   ],
   "commands": "Worktree /private/tmp/claude-501/.../scratchpad/review/C3-build-r1/gglib at be39258c (detached), with web_ui copied in; target rv-gglib-proxy with CARGO_PROFILE_DEV_DEBUG=line-tables-only CARGO_INCREMENTAL=0. The worktree was removed at the end, and the target (16G) is left in place.\n\nResults at the tip:\n- cargo clippy --workspace --all-targets --all-features -- -D warnings, native aarch64-apple-darwin: exit 0, all 18 members checked.\n- The same command with --target x86_64-unknown-linux-gnu, cross-checked from macOS with zig cc and a fake pkg-config (check only): exit 0.\n- The same command with --target x86_64-pc-windows-gnu (zig cc, llvm-windres): exit 0.\n- I could not clippy x86_64-pc-windows-msvc (CI's windows-2022), native ubuntu with real GTK/WebKit (CI's clippy job), or the macos-14 runner itself. Only CI covers those. git grep finds no target_env anywhere, so windows-gnu sees the same gglib cfgs as msvc.\n- cargo test --workspace --no-fail-fast: exit 0, 111 result lines, 4113 passed, 0 failed, 70 ignored. The author's numbers match.\n- Also exit 0: cargo fmt --all --check; check_rust_complexity.sh; check_lint_inheritance.sh (and under /bin/bash 3.2); --self-test; shellcheck; make enforce; make doc-check (RUSTDOCFLAGS=-D warnings); make bindings-check (199 types, no diff); check_boundaries.sh; check_workflow_yaml.sh; check_readmes.sh --strict; check_readme_tables.py; check_ts_bindings.sh; check_adrs.py --check.\n- Per commit (ca7e3b81, a9bda20a, 12eac4ac, be39258c): fmt and complexity pass on each; the lint script passes on 12eac4ac and be39258c.\n- Attribution grep prints 0. Author and committer are Matt O'Grady <sewing.wader8c@icloud.com> on all 4 commits.\n\nVerified claims:\n- All 18 members have [lints] with only workspace = true.\n- 213 added allow attributes (212 in a9bda20a plus layer_shell), all with a reason, none crate-level. Counts: 319 lint names = 318 + unsafe_code; baseline totals 505/182. Counts are 186/182 at base and 187/182 after commit 1, so no bare allow was added.\n- Commit 2 adds only attribute lines, apart from setup_app.\n- The K7 comments are 36\u21924 and 9\u21923 lines; both are true and state the rule plus one sentence.\n- missing_const_for_fn really is target-dependent: metal.rs:17 `has_metal_support` and dock.rs:82 are const-eligible only off macOS.\n- Spot-checks of the --fix diff found no behaviour change: if_not_else swaps (server.rs:815, remote/mod.rs print_status, cache_lifecycle.rs:83, question_input.rs, health.rs), redundant_else, map_unwrap_or, bool_to_int_with_if, implicit_clone (residency/launch.rs:247), from_mins/from_hours (toolchain 1.97.1), 13 Eq derives, PoisonError method paths in lease.rs.\n- Mod-level allows: most children are at their cap. Those with spare lines (axum config/setup.rs, process/core.rs, cache_lifecycle.rs) have findings spread over too many functions to fit.\n- No fenced block was removed.",
   "mutations": "Script against the real tree. Each ran ./scripts/check_lint_inheritance.sh, then the tree was restored with git checkout:\n- proxy Cargo.toml with [lints.clippy] appended: rc=1, \"declares [lints.clippy]\".\n- runtime `workspace = false`: rc=1, \"[lints] holds `workspace = false`\".\n- src-tauri [lints] removed: rc=1, \"has 0 [lints] tables\".\n- axum manifest restored to base: rc=1, \"declares [lints.rustdoc]\".\n- cli extra key under [lints]: rc=1, \"holds `clippy = {...}`\".\n- core [lints.rust] after a commented `workspace = true`: rc=1.\n- Bare allow in proxy lib.rs: rc=1, \"10 ... without a reason, baseline 9\".\n- Reasoned allow: rc=1, \"79 lints allowed, baseline 78\".\n- cfg_attr reasoned allow: rc=1.\n- Bare expect: rc=1.\n- Crate-root allow in integration-tests/tests/*.rs: rc=1, \"6 lints allowed, baseline 5\".\n- src-tauri build.rs bare allow: rc=1.\n- `--update` with a bare allow: refused (\"baseline not updated\").\n- `--update` with a reasoned allow: baseline 78\u219279, then the check passes.\n- make enforce with [lints.rust] added to db: exit 2.\n- Survived: `# [allow(...)]` with a space passes the script. Low risk, because rustfmt rewrites it to `#[allow` outside macro bodies.\n- Survived: a bare allow in crates/build_common.rs, rc=0 (defect 3).\n\nSelf-test against script mutations, each run with --self-test:\n- cfg_attr ignored: fails.\n- Comments counted: fails.\n- [lints.x] not flagged: fails.\n- Bare count dropped: fails.\n- Lints growth allowed: fails.\n- --update may raise the bare count: fails.\n- Survived: `find \"$1/src\"`, which drops tests/*.rs. Both the self-test and the real check pass, and a new bare tests/ crate-root allow then passes too (defect 2).\n\nClippy behaviour:\n- Workspace `missing_const_for_fn = \"allow\"` removed: 74 \"could be a const fn\" warnings on macOS, matching the author's mac count.\n- layer_shell `unsafe_code` allow removed: macOS clippy -D warnings still passes (cfg-gated, macOS cannot see it); Linux cross clippy fails with 6 unsafe_code errors at layer_shell.rs:60,63,88,90,120,199.\n- Allows on residency, supervisor, admission `state`, sampling_audit and token_calibration removed: the significant_drop_tightening, cast and other findings listed in defect 1 reappear.\n- Allows on `mod tune` and `mod scoring_tests` removed: the scoring_tests float_cmp and needless_pass_by_value findings reappear. With only `mod scoring_tests`'s allow removed, clippy stays silent, so that allow is redundant (defect 4).\n- Allow on `mod remote` removed: findings reappear at remote/mod.rs:228 and connect.rs:83/87/149/183 (connect.rs is 298/300, so the move up to `mod remote` is justified).\n- The behaviour of the --fix rewrites is covered by the full test suite passing (4113 passed). No new tests were added, and none were expected for a lint-only PR."
  },
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "head_sha": "23ce2fc21ed7b40cd5074f3888a95bbfd77e60bb",
   "base": "tmp/c3-base",
   "commits": [
    "ca7e3b81 chore(lints): every crate inherits the workspace lints (unchanged)",
    "8d0ef25c chore(lints): the remaining findings are grandfathered where they stand (was a9bda20a; fixup folded, message reworded)",
    "a4329928 chore(lints): enforcement checks lint inheritance and each crate's allow count (was 12eac4ac; fixup folded, message reworded)",
    "23ce2fc2 chore(lints): the visibility lints' comments state the rule (was be39258c; content unchanged, rebased)"
   ],
   "summary": "All five defects are fixed in worktree /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib. Each fix is folded into its own commit with the subject kept. Nothing was pushed.\n\nDefect 1 (8d0ef25c). The lint `significant_drop_tightening` now has its own `#[allow]` on `mod residency` (runtime process/mod.rs), `mod supervisor` (runtime proxy/mod.rs), `mod sampling_audit` and `mod token_calibration` (proxy lib.rs), and `mod common` (proxy tests/fixtures/mod.rs). The three loop_guard_step_trips_tests allows get the same reason. That reason reads: \"a guard's scope is its critical section, so this lint is never applied in admission, residency or proxy lock code\". `cast_possible_truncation` on admission's `mod state` is split out with the reason \"a wait's milliseconds pass u64::MAX only after 584 million years\". The other lints stay grandfathered. A mutation that removed these new allows brought back exactly the 14 findings the reviewer listed (residency 610, launch 174/285, supervisor 312/463/512, sampling_audit 770, token_calibration 367, loop_guard 78/97/98/139, fixtures/common 633, state 737). Other significant_drop allows outside the plan's areas stay grandfathered on purpose: app-services remote/servers, db, cli target_tests, runtime logs and src-tauri state_sync.\n\nDefect 4 (8d0ef25c). I built a syn-based scan of every enclosing scope. It found the reviewer's six redundant attributes plus a seventh, props_parse_tests.rs `real_params` (its lint is already allowed by `mod props` in proxy lib.rs). All seven are deleted; on stored_pairing `settle` only `single_match_else` goes and `similar_names` stays. Re-running the scan finds no overlap from this PR. The only overlaps left are the `dead_code` allows in gglib-agent tests/common, which are already on the base branch.\n\nDefects 2 and 3 (a4329928). scripts/check_lint_inheritance.sh now counts .rs files outside every member directory in a baseline row `(outside-members)`, and a failure there names the files. The walk skips target/, node_modules, .git and the member directories. The self-test fixtures now cover:\n- good/tests/it.rs and good/build.rs;\n- goodies/shared.rs, a sibling whose name starts with the member name `good`;\n- target/ files at the root and inside a member, which must not be counted;\n- a final `--update` step that must rewrite the baseline to the exact counts.\n\nThe header's exit-code line now includes \"2 usage\". scripts/README.md is updated. With `--update` the baseline goes to app-services 52 (was 60), proxy 77 (was 78) and `(outside-members) 0 0`: 496 lints and 182 bare in total, was 505 and 182.\n\nDefect 5. The 8d0ef25c message now says \"212 attributes naming 309 lints, on 142 functions, 7 structs, a static, a `use`, and 54 `mod` items\". A script recounted this from the diff, including 7 items that carry two attributes. The message also lists the 14 attributes with their own reason (8 lock code, 1 cast, 5 Tauri). The a4329928 message gives the new baseline totals, describes the outside row and the new self-test, and says fourteen mutations.\n\nTree identity: after the autosquash the tree is 85cfdf52, the same as the tree with the fixups on top; the reword kept it. Every commit is authored and committed by Matt O'Grady <sewing.wader8c@icloud.com>. The attribution grep prints 0.",
   "commands_run": "All run in the lane worktree; the target is the lane's symlink under scratchpad/targets/gglib-proxy.\n\nAt the tip (23ce2fc2):\n- `cargo fmt --all`: clean.\n- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0 on aarch64-apple-darwin, and cross-checked from macOS with zig cc on x86_64-unknown-linux-gnu and x86_64-pc-windows-gnu, exit 0 on both. Logs are in scratchpad/c3/r2/clippy-{mac,linux,win}.log; each log's first line is the HEAD hash, and the worktree had 0 uncommitted changes.\n- `./scripts/check_lint_inheritance.sh`: exit 0. `--self-test`: exit 0. Under /bin/bash 3.2: exit 0. shellcheck: 0.\n- `make enforce`: exit 0. `check_boundaries.sh`: 0. `check_readmes.sh --strict`: pass. `check_rust_complexity.sh`: pass.\n\nTargeted tests:\n- `cargo test -p gglib-proxy --lib -- loop_guard_step_trips props_parse`: 7 passed.\n- `cargo test -p gglib-app-services --lib -- scoring`: 15 passed.\n- `cargo test -p gglib-runtime --lib -- admission residency`: 84 passed.\n\nPer commit, in a temporary detached worktree (since removed, 0 uncommitted changes each): fmt --check and check_rust_complexity pass on all four commits. check_lint_inheritance, shellcheck and bash 3.2 pass on a4329928 and 23ce2fc2.\n\nAlso run:\n- A scratch overlap scanner (syn) over all 109 crate roots.\n- A recount script over the 8d0ef25c diff.\n- A tree comparison after the autosquash and after the reword: same tree both times.\n- The commit messages were read back and diffed against the files they were written from: identical.",
   "mutations": "Lint script, each run with `--self-test` on a mutated copy (script at scratchpad/c3/r2/script_mut.py). All 14 were killed (rc=1), and the unmutated script passes:\n1. cfg_attr ignored\n2. comments counted\n3. [lints.x] not flagged\n4. extra key under [lints] not flagged\n5. bare count dropped\n6. growth in the lint count allowed\n7. `--update` may raise the bare count\n8. `find \"$1/src\"`, the reviewer's defect-2 mutation, now killed by \"an allow above the baseline passed\"\n9. files outside every member not counted\n10. root target/ counted\n11. a member name matching a sibling directory that shares its prefix\n12. a member's target/ counted\n13. reason counted as a lint\n14. string literals not blanked\n\nReal tree (script at scratchpad/c3/r2/tree_mut.py; each file restored by rewriting its bytes and touching it):\n- Bare `#[allow(clippy::too_many_lines, clippy::pedantic)]` in crates/build_common.rs: rc=1, \"(outside-members): 2 lints allowed without a reason, baseline 0 / counted in crates/build_common.rs\".\n- A reasoned allow there: rc=1, \"(outside-members): 1 lints allowed, baseline 0\".\n- `--update` with a bare allow there: refused (\"baseline not updated\").\n- Bare crate-root allow in integration-tests/tests/integration_models.rs: rc=1 (6 > 5).\n- Reasoned allow in runtime build.rs: rc=1 (91 > 90).\n- Clean tree: rc=0.\n\nClippy: with the split lock-code and cast allows removed from runtime and proxy, `cargo clippy -p gglib-proxy -p gglib-runtime --all-targets --all-features` reports exactly the 14 findings in the summary; with the allows restored, clippy -D warnings passes. With the redundant inner attributes deleted, clippy passes on all three targets, so the enclosing `mod` allows are the ones that take effect.",
   "deviations": "- Defect 1: fixed. The significant_drop_tightening allows on residency, supervisor, sampling_audit, token_calibration and fixtures `common`, plus the three loop_guard test allows, now carry the rule as their reason, split into their own attributes. cast_possible_truncation on admission `mod state` is split with a reason about milliseconds. The reason names the plan's scope (\"admission, residency or proxy lock code\") and does not say \"lock code\" in general, because significant_drop allows in app-services remote/servers, db, cli target_tests, runtime logs and src-tauri state_sync stay grandfathered. #1157's body is NOT edited: the brief forbids GitHub writes. The replacement body, which also fixes the stale sentence about \"counts the `allow(clippy::` occurrences\", is in /private/tmp/claude-501/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6/scratchpad/c3/r2/issue-1157-body.md for the publisher to apply.\n- Defect 2: fixed. The self-test has fixtures in tests/ and build.rs, plus an exact-count `--update` step. The `find \"$1/src\"` mutation now fails the self-test.\n- Defect 3: fixed. There is an `(outside-members)` baseline row, a failure there names the files, and a fixture sits outside every member. A bare allow in crates/build_common.rs now fails with rc=1.\n- Defect 4: fixed, and widened by one. All six attributes the reviewer named are deleted; on stored_pairing only `single_match_else` is removed and `similar_names` stays. My scan found a seventh redundant attribute the reviewer missed, on props_parse_tests.rs `real_params`, already covered by `mod props`, and it is deleted too. The baseline drops by 9 lints (app-services 60\u219252, proxy 78\u219277).\n- Defect 5: fixed. The 8d0ef25c message now counts items, not attributes: \"212 attributes naming 309 lints, on 142 functions, 7 structs, a static, a `use`, and 54 `mod` items\". The header's exit-code line adds \"2 usage\".\n- The PR body does not exist yet, since the publisher writes it. It should use these numbers: 212 attributes, 309 lints, 142 functions, 54 mod items, and a baseline of 496 lints / 182 bare with `(outside-members) 0 0`. Do not use 318, 148, 55 or 505.",
   "review_focus": "- The reason text on the eight lock-code allows is a policy claim scoped to the plan's areas. Check the reviewer accepts scoping it to \"admission, residency or proxy lock code\" while app-services remote's significant_drop allows stay grandfathered and payable.\n- Loop_guard_step_trips_tests and proxy tests/fixtures/common.rs are test code in the proxy crate. I treated them as proxy lock code, as the reviewer's defect listed them.\n- outside_files walks the repository root and skips target, node_modules, .git and each member directory. An untracked local .rs file outside the members would be counted locally but not in CI. A member entry with glob characters would already fail with \"no Cargo.toml\".\n- A new sentence in 8d0ef25c's message says no attribute in that commit allows a lint that an enclosing `mod` item already allows. It rests on the syn scan in scratchpad/c3/overlap, which follows `mod` files, `#[path]`, inline mods, fn bodies, impl and trait items, locals, fields and variants. It does not look at attributes on expressions.\n- CI-only coverage is unchanged: x86_64-pc-windows-msvc, native ubuntu with real GTK/WebKit, and the macos-14 runner."
  },
  {
   "verdict": "FAIL",
   "defects": [
    {
     "where": "scripts/check_lint_inheritance.sh:121-127 (outside_files), reached from check() at :173",
     "what": "outside_files walks the filesystem from the repository root. It prunes only target, node_modules, .git and the member directories. It counts every gitignored .rs file too. Your main checkout /Users/mattogrady/.local/src/gglib has the gitignored .claude/worktrees/wf_fd0a6660-fa6-2, a Claude Code worktree nested inside the repo, with 977 .rs files. I ran the tip script unchanged except ROOT_DIR set to that checkout and BASELINE set to the tip baseline. It exited rc=1 with '\u274c (outside-members): 179 lints allowed without a reason, baseline 0' and 978 'counted in' lines. `--update` cannot get past it, because the bare count may not rise. So `make enforce` fails in the main checkout whenever a Claude worktree is open there, while CI passes. The author's review_focus called this case hypothetical ('an untracked local .rs file'). It happens today.",
     "fix": "List files through git, not find: `git -C \"$root\" ls-files -z --cached --others --exclude-standard -- '*.rs'`. Assign each path to the member whose directory it sits in (path starts with \"<member>/\"); everything else goes to (outside-members). Remove the find pruning in outside_files and count_member. In selftest, run `git init -q \"$dir\"` and write a .gitignore holding `target/` and `.claude/`. Add a must-not-count fixture under `.claude/worktrees/x/crates/y/src/lib.rs` holding a bare allow. Update the header, scripts/README.md and the a4329928 message to say which files are counted (tracked plus untracked-but-not-ignored)."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:112 (count_member: -not -path '*/target/*' -not -path '*/node_modules/*')",
     "what": "The exclusion drops every directory named target or node_modules at any depth inside a member, not just build output. Tree mutation: I added crates/gglib-cli/src/target/mod.rs holding `#![allow(clippy::pedantic, clippy::nursery)]` and `#[allow(unsafe_code)]`, which is 3 bare lints. The script printed '\u2705 every member inherits the workspace lints\u2026'. Moving the file to crates/gglib-cli/tests/target/x.rs, and a src/node_modules/a.rs, also passed. gglib-cli already has target.rs, target_remote.rs, target_use.rs and target_tests.rs, which is the shape the file-size ratchet turns into a `src/target/` directory module. Every allow under that directory would then go uncounted. The header (:12-14) and scripts/README.md say every lint in a member's .rs files is counted. Neither mentions this exclusion.",
     "fix": "The git ls-files enumeration from defect 1 fixes this too, because target/ is gitignored. If you keep find, exclude only \"$1/target\" and \"$1/node_modules\", not '*/target/*'. Add a self-test fixture good/src/target/mod.rs with one bare allow that MUST be counted and raise the expected `good` row to match. Keep the root target/ fixture as must-not-count."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:101-105 (lints_problem END block); selftest fixtures :221-226 and assertion :266-268; header claim :31-33",
     "what": "No self-test manifest lacks a [lints] table or says `workspace = false`, so the END-block detector is untested. Script mutation: I emptied the END block (`if (tables != 1) \u2026 else if (inherit != 1) \u2026`). `--self-test` still printed \u2713 with rc=0. The mutated script also passed a workspace whose only member has no [lints] table (rc=0). That is src-tauri's state before this PR. Dropping only the `workspace = false` branch also survived. The header (:31-33) says the self-test has 'fixtures that must fail each rule\u2026 so a detector that stops detecting fails here'. This detector can stop detecting while the self-test passes.",
     "fix": "Add two bad fixtures to the first self-test pass: a member manifest with no [lints] table, and one with `[lints]\\nworkspace = false`. Assert their messages ('has 0 [lints] tables, not one' and '[lints] does not say `workspace = true`'), and change the `-ne 2` count to `-ne 4`. Fix both manifests in the clean pass, as the table and extra manifests already are. Update the \u2713 line, the scripts/README.md sentence 'two manifests that must fail rule 1', and the a4329928 message sentence 'It has two manifests that must fail'. Then rerun the 14 script mutations plus the END-block one."
    }
   ],
   "commands": "All commands ran in my worktree /private/tmp/claude-501/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6/scratchpad/review/C3-build-r2/gglib, detached at 23ce2fc2 with dirty=0. Every log's first line is the commit hash. The target was scratchpad/targets/rv-gglib-proxy with CARGO_PROFILE_DEV_DEBUG=line-tables-only CARGO_INCREMENTAL=0. The worktree has been removed; the target (16G) is left in place.\n\nClippy:\n- `cargo clippy --workspace --all-targets --all-features -- -D warnings` on aarch64-apple-darwin: rc=0. All 18 crates were re-checked; the only warnings are the 36 existing ts-rs \"failed to parse serde attribute\" notes.\n- The same command on x86_64-unknown-linux-gnu and x86_64-pc-windows-gnu, cross-compiled from macOS with my own copy of the zig cc wrappers and a fake pkg-config: rc=0 on both.\n- CI-only: x86_64-pc-windows-msvc, native ubuntu with real GTK/WebKit, and the macos-14 runner. No target_env cfg exists in the tree.\n\nTests and gates:\n- `cargo test -p gglib-proxy -p gglib-runtime -p gglib-app-services -p gglib-db -p gglib-axum -p gglib-cli -p gglib-tauri --no-fail-fast`: 68 binaries, 2167 passed, 0 failed, 50 ignored.\n- `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps --document-private-items --exclude gglib-app`: rc=0.\n- `make bindings-check`: 199 types, the bindings match.\n- `make enforce`: rc=0. `check_boundaries.sh`: rc=0. `cargo fmt --check`: rc=0. `check_adrs.py --check`: rc=0. `check_rust_complexity.sh`: pass.\n- `check_lint_inheritance.sh`: rc=0. `--self-test`: rc=0. /bin/bash 3.2: rc=0. `--bogus`: rc=2. shellcheck: 0.\n- `--update` rewrites the baseline byte-identical, so the baseline is exact.\n- Per commit (ca7e3b81, 8d0ef25c, a4329928, 23ce2fc2): fmt and complexity pass. The lint script passes on a4329928 and 23ce2fc2.\n- Attribution grep: 0. Author and committer are Matt O'Grady <sewing.wader8c@icloud.com> on all four commits.\n\nIndependent verification:\n- Allow counter: my own Python counter, which also strips block comments and raw strings, gives the same numbers as scripts/lint-allow-baseline.txt on every row: 496/182, with (outside-members) 0 0. At base the total is 186 lints and 182 bare, so the PR adds 310 lints (309 in 8d0ef25c plus unsafe_code) and no bare ones.\n- Added attributes: diffing base against tip gives 213 attributes. 198 say \"grandfathered at lint inheritance, #1157\". The other 15 have their own reasons: 8 lock-code, 1 cast, 5 Tauri and 1 unsafe_code. The 212 in 8d0ef25c sit on 205 items (142 fn, 54 mod, 7 struct, 1 static, 1 use), and 7 items carry two attributes. The commit message's numbers are correct.\n- No `#![allow]` was added. Every member's [lints] is exactly `workspace = true`. The removed per-crate tables held nothing stricter than the workspace.\n- Overlap scanner (my own; module tree, #[path], enclosing items): on the round-1 tree be39258c it finds the reviewer's 6 overlaps plus real_params. At the tip it finds only the overlaps already on the base branch. Defect 4 is fixed.\n- Expect conversion: I turned all 213 added allows into `expect` and ran clippy on 3 targets. Every expectation is fulfilled in some unit on some target (the ones unfulfilled in the lib unit fire in the test unit), so no allow is unneeded.\n- Stripping all 212 added allows leaves 489 findings under the author's key (crate, lint, file:line:col), which matches the message.\n- The scope of every mod-level allow was checked against file-size headroom. Files with headroom needed more attribute lines than they had.\n- Sampled the --fix diffs: 28 duration unit changes (all equal values), all map_or/is_some_and rewrites, all method-path closures (including PoisonError::into_inner in admission/lease.rs), 14 semicolon additions (log macros only, edition 2024), `_ = \u2026` \u2192 `() = \u2026` in select!, and derive(Eq) additions. No behaviour change found. needless_borrow is not in the fix set; the crates were already clean on clippy's default lints.\n- The K7 comments are true. The old comments ran 36 and 9 lines, as the message says, and 23ce2fc2 changes comment lines only.\n\nRound-1 defects:\n1. Fixed. Eight significant_drop_tightening allows carry the lock-code reason: mod residency, mod supervisor, sampling_audit, token_calibration, fixtures common and the three loop_guard tests. The cast on admission `mod state` is split out and its reason is accurate for state.rs:737. No grandfathered attribute covers a significant_drop finding in admission, residency or proxy code.\n2. Fixed. The tests/ and build.rs fixtures and the exact-count `--update` step are in place.\n3. Fixed as specified. The (outside-members) row exists, and crates/build_common.rs is its only tracked file. The repair introduced new defect 1.\n4. Fixed.\n5. Fixed.\n\nNon-blocking notes:\n- A trailing `// \u2026` comment inside an allow list, before `reason =`, is counted as a lint and makes the attribute bare. This is a false failure, and nothing at the tip triggers it.\n- The stale-baseline-row check and the nothing-scanned guard have no self-test fixture.\n- The #1157 body still says the script counts \"`allow(clippy::` occurrences\". The author's replacement body is waiting for the publisher.",
   "mutations": "Tree mutations (each file restored by rewriting its bytes and touching it; git status clean afterwards):\n- M1: gglib-proxy [lints] gains `clippy = { pedantic = \"allow\" }`. rc=1, \"[lints] holds `clippy = \u2026`\".\n- M2: [lints] replaced by [lints.clippy]. rc=1, \"declares [lints.clippy]\".\n- M3: [lints] removed. rc=1, \"has 0 [lints] tables, not one\".\n- M4: reasoned allow added to runtime admission. rc=1, \"91 lints allowed, baseline 90\".\n- M5: bare allow added. rc=1, \"18 lints allowed without a reason, baseline 17\". `--update` then refuses (\"baseline not updated\").\n- M6: reason stripped from the admission cast allow. rc=1, bare 18 > 17.\n- M7 (hole, new defect 2): a bare allow in crates/gglib-cli/src/target/mod.rs, in tests/target/x.rs, or in src/node_modules/a.rs. rc=0 in every case: SURVIVED.\n- Main-checkout simulation (new defect 1): the tip script with ROOT_DIR set to ~/.local/src/gglib gives rc=1, \"(outside-members): 179 \u2026 baseline 0\", from the 977 files in .claude/worktrees.\n\nClippy mutations, each on 3 targets:\n- All significant_drop_tightening allows removed: 22 findings. Every one in admission, residency or proxy code sits under a lock-code-reason attribute. The rest are in app-services remote/servers, cli target_tests, runtime logs, src-tauri state_sync and the existing core model_service.\n- All 213 added allows turned into expect: every expectation is fulfilled on some unit and target.\n- All 212 added allows removed: 489 findings, matching the commit message.\n\nScript mutations, each run with `--self-test` on a mutated copy. KILLED (rc=1):\n- ignore cfg_attr\n- count only src/\n- strings not blanked\n- comments counted\n- reason counted as a lint\n- bare = every lint\n- bare never counted\n- extra key in [lints] accepted\n- [lints.x] accepted\n- lints growth allowed\n- bare growth allowed\n- update may raise bare\n- outside members skipped\n- member target/ counted\n- root target/ counted\n- member pruned by prefix\n- members parser reads only the first\n- expect not counted\n- update writes nothing\n\nSURVIVED (rc=0):\n- END block emptied (new defect 3). The mutated script also passes a real workspace whose member has no [lints].\n- `workspace = false` accepted (new defect 3).\n- Stale baseline row accepted (minor).\n- Zero-scan guard removed (minor).\n- Missing-table line only (equivalent mutant; the inherit check still catches it).\n\nThe unmutated script passes."
  },
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "head_sha": "63638c4ebaddd92b3b2ae59e4372a0535170a95d",
   "summary": "I fixed all three defects in scripts/check_lint_inheritance.sh and scripts/README.md. The fix went into the script commit as a fixup, then autosquash. That commit's message was rewritten, and the subjects are unchanged. Not pushed.\n\nCommits on tmp/c3-base:\n- ca7e3b81 chore(lints): every crate inherits the workspace lints (unchanged)\n- 8d0ef25c chore(lints): the remaining findings are grandfathered where they stand (unchanged)\n- e92fa547 chore(lints): enforcement checks lint inheritance and each crate's allow count (was a4329928)\n- 63638c4e chore(lints): the visibility lints' comments state the rule (was 23ce2fc2; same diff, replayed)\n\nHEAD^{tree} equals the tree of the fixup commit I built (4bdec11c). `git diff a4329928 e92fa547` touches only scripts/README.md and scripts/check_lint_inheritance.sh. The raw message of e92fa547 matches my message file byte for byte. The attribution grep prints 0, and author and committer are Matt O'Grady <sewing.wader8c@icloud.com> on all four commits.\n\nWhat changed in the script:\n- **File listing (defects 1 and 2).** find is gone. `rs_files` (:121-129) runs `git -C root ls-files -z --cached --others --exclude-standard --deduplicate`, keeps `*.rs`, and drops paths missing from the working tree. `check` reads the members and the files once each. A member's files are the paths that start with \"<member>/\"; any file matching no member goes to the (outside-members) row.\n- **Git environment.** The script first runs `unset $(git rev-parse --local-env-vars)` (:45). I measured the need: a pre-commit hook in a linked worktree exports GIT_DIR and an absolute GIT_INDEX_FILE. With those set and the unset line removed, the self-test still passed but wrote good/src/gone.rs, lib.rs and merge.rs (at three stages) into the caller's index. With the line in place, the caller's index is untouched.\n- **Self-test.** It runs `git init` in the temp dir and writes a .gitignore of `/target` and `.claude/`. It has five bad manifests: [lints.clippy], a second key, no [lints] table, an empty [lints] table, and `workspace = false`. It also has a member `lost` with no manifest. Files with known counts:\n  - src/lib.rs, tracked: 6 lints, 3 bare\n  - src/merge.rs, in the index at stages 1-3: 1/0\n  - src/target/mod.rs, which must be counted: 1/1\n  - tests/it.rs: 2/1\n  - build.rs: 1/0\n  - goodies/shared.rs, outside every member: 1/1\n- **Must-not-count fixtures.** target/gen.rs, .claude/worktrees/x/crates/y/src/lib.rs, a tracked file deleted from the working tree, and good/README.md. The expected good row is 11 5.\n- **New assertions.** A baseline row that names no member must fail, and so must a tree where git lists no .rs file.\n\nThe header, scripts/README.md and the e92fa547 message now say which files are counted: tracked, or untracked and not ignored.",
   "commands_run": "All runs were in /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib at tip 63638c4e with dirty=0, unless a line says otherwise.\n\nLint script:\n- `./scripts/check_lint_inheritance.sh`: rc=0, \"\u2705 every member inherits\u2026\".\n- `--self-test`: rc=0, \"\u2713 self-test: five bad manifests and a missing one flagged; 11 lints and 5 without a reason \u2026\".\n- `/bin/bash` (3.2.57) for the check and `--self-test`: rc=0.\n- `--bogus`: rc=2.\n- `shellcheck scripts/check_lint_inheritance.sh`: rc=0, no findings.\n- `--update`, then `cmp` against the saved baseline: byte-identical. The baseline is unchanged: 496/182, with (outside-members) 0 0.\n\nOther checks:\n- File set: git lists 1018 .rs files, and the old find (pruning target, node_modules and .git) finds the same 1018, so the counts on this tree are unchanged.\n- `make enforce`: rc=0, 0 \u274c lines.\n- `python3 scripts/check_adrs.py --check`: rc=0.\n- Per commit: a detached scratch worktree at e92fa547 (dirty=0) gives rc=0 for the script. The worktree was removed and pruned afterwards.\n- No Rust changed in this round, so cargo fmt, clippy and tests were not rerun.\n\nMain-checkout simulation. This was read-only: GIT_OPTIONAL_LOCKS=0, a copy of the script with ROOT_DIR=~/.local/src/gglib and BASELINE set to the lane baseline. That checkout is at f07d7895 and holds 977 .rs files under the ignored .claude/.\n- Old script (23ce2fc2): rc=1, \"\u274c (outside-members): 179 lints allowed without a reason, baseline 0\", and 978 \"counted in\" lines. This reproduces the reviewer's result.\n- New script: no (outside-members) failure and 0 \"counted in\" lines. The only \u274c lines are the eight expected \"declares [lints.rustdoc]\" failures, because that checkout predates this PR.\n\nGit behaviour I checked before relying on it:\n- A merge conflict makes `ls-files --cached` print a path three times, so the listing needs `--deduplicate`.\n- An untracked nested repo is listed as \"dir/\", so the `*.rs` filter drops it.\n- In a linked worktree a pre-commit hook gets GIT_DIR and an absolute GIT_INDEX_FILE. `commit -a` and `commit <path>` both give it index.lock or next-index-*.lock.",
   "mutations": "Script mutations. Each ran as `bash <mutated copy> --self-test`. The copies were written under the scratchpad, so the tree was never edited. The unmutated script gave rc=0 before and after the run. Result: 33 of 33 KILLED (rc=1), 0 SURVIVED. The failing assertion is in brackets.\n\nCounter and rule 1:\n- ignore cfg_attr [allow above baseline]\n- expect not counted [allow above baseline]\n- strings not blanked [six bad manifests]\n- comments counted [six]\n- reason counted as a lint [six]\n- bare = every lint [six]\n- bare never counted [outside allow passed]\n- [lints.x] accepted [six]\n- extra key accepted [six]\n- END block emptied [six] (this survived before)\n- END tables branch only [six]\n- END inherit branch only [six] (this survived before)\n- `workspace = <anything>` accepted [six]\n\ncompare and --update:\n- lints growth allowed [allow above baseline]\n- bare growth allowed [outside allow passed]\n- --update may raise bare [\"--update raised\"]\n- --update writes nothing [exact counts]\n\nFile listing:\n- drop --cached [allow above baseline]\n- drop --others [allow above baseline]\n- drop --exclude-standard [six]\n- drop --deduplicate [six]\n- deleted files listed [allow above baseline]\n- non-.rs files listed [six]\n- count only src/ [allow above baseline]\n- member held by a bare prefix [six]\n- outside members skipped [outside]\n- outside held by a bare prefix [outside]\n\nMembers parser, stale row and guards:\n- members parser reads one path per line [six]\n- members parser stops at its first line [six]\n- members comment not stripped [six]\n- stale baseline row accepted [stale row] (had no fixture before)\n- zero-scan guard removed [empty listing] (had no fixture before)\n- missing manifest accepted [six]\n\nTree mutations on the real check. Each file was untracked and deleted afterwards; git status was clean at the end.\n- A bare `#![allow(clippy::pedantic, clippy::nursery)]` + `#[allow(unsafe_code)]` in crates/gglib-cli/src/target/mod.rs: rc=1, \"crates/gglib-cli: 15 lints allowed without a reason, baseline 12\". This was the reviewer's M7, which survived before.\n- The same allows in crates/gglib-cli/tests/target/x.rs: rc=1, same line.\n- The same allows in crates/gglib-cli/src/node_modules/a.rs, untracked: rc=0, because .gitignore:53 `node_modules` ignores it (check-ignore confirms). After `git add -f` it gave rc=1, 15 > 12, and I then reset it.\n- .claude/worktrees/x/crates/y/src/lib.rs with the same allows: rc=0, ignored by .gitignore:92.\n- An untracked crates/stray.rs: rc=1, \"(outside-members): 3 \u2026 baseline 0\", naming stray.rs.",
   "deviations": "- **Defect 1: fixed.** Files are listed by git (`ls-files --cached --others --exclude-standard --deduplicate`), and the find pruning is gone. The header, scripts/README.md and the e92fa547 message say which files count. I made two additions:\n  - `--deduplicate`: a conflicted path appears three times without it (measured).\n  - A skip for tracked files missing from the working tree: perl would die on them.\n  - Both have fixtures and killed mutations.\n- **Defect 2: fixed, with one deviation.** The self-test's .gitignore holds `/target`, as the repo's .gitignore:2 does, instead of the suggested `target/`. An unanchored `target/` would also ignore good/src/target/, so that must-count fixture could never be counted. The old good/target/debug/gen.rs fixture is gone: under `/target` a member's target/ is not ignored, in the fixture or in this repo. The root target/ fixture stays as must-not-count.\n  - src/node_modules/*.rs is still not counted while untracked, because .gitignore:53 `node_modules` is unanchored and git ignores it. `git add` refuses such a file without -f, and a force-added one is tracked and counted (measured rc=1).\n- **Defect 3: fixed, with one deviation.** `workspace = false` never reaches the END block. awk line :107 reports it as \"[lints] holds `workspace = false`\". The END block's inherit branch (:112) is reached by an empty [lints] table.\n  - So the self-test has three new manifests (no table, an empty table, and workspace = false), each with its own message assertion, plus a member with no manifest. The count is `-ne 6`, not `-ne 4`.\n  - The END-block and inherit-branch mutations are both now killed.\n- **Beyond the defects (my own choices, open to review):**\n  - Fixtures for the stale baseline row and the zero-scan guard (the reviewer's non-blocking notes). The guard matters more now, because a git failure yields an empty listing.\n  - A non-.rs fixture.\n  - A missing-manifest fixture.\n  - The `unset $(git rev-parse --local-env-vars)` line, justified by the measured index pollution.\n- **Not done:** the non-blocking note that a trailing `//` comment inside an allow list counts as a lint. Nothing in the tree triggers it, and it is unchanged.",
   "review_focus": "- **Line :45, `unset $(git rev-parse --local-env-vars)`.** It is new and was not requested. Check that it cannot hurt the real check. ROOT_DIR comes from the script's own location, so the repo is found from the root. The GIT_DIR, GIT_INDEX_FILE and pollution claims in the comment and the message are measured, by setting the variables by hand to what a linked-worktree hook exports. I did not run it from inside a real hook.\n- **CI now needs git and a git checkout.** The enforcement job uses actions/checkout@v7 on ubuntu-latest, which is a git repo. If git fails there (\"dubious ownership\", or a git older than 2.31 without `--deduplicate`), the listing is empty. Only the zero-scan guard then fails the run, and git's stderr says why. The guard now has a fixture.\n- **Pre-existing gap, not fixed.** If perl dies on an unreadable file, `count` prints nothing, `[ \"\" -gt N ]` errors and evaluates false, and that row passes in check mode. Only the exact-count self-test step catches a broken counter, and only for its fixtures. The new skip for missing files closes the one case this PR made likely: a tracked file deleted from the working tree. An unreadable file would still pass silently.\n- **Behaviour change beyond the defects.** An ignored-but-untracked .rs file is no longer counted anywhere, including under an unanchored `node_modules`. A tracked file is counted even when an ignore pattern matches it. The documents say \"tracked, or untracked and not ignored\".\n- **The e92fa547 message** has new claims to check: 977 files under the ignored .claude/ of the main checkout, \"Thirty-three mutations\", and the fixture counts (5 lints and 2 bare across tests/, build.rs, src/target/ and the three-stage file).\n- **Carried over from before:** the #1157 body still describes counting `allow(clippy::` occurrences. That is for the publisher."
  },
  {
   "verdict": "FAIL",
   "defects": [
    {
     "where": "scripts/check_lint_inheritance.sh:228-307 (selftest). The must-count fixture good/src/target/mod.rs is at :280 and the listing is at :123.",
     "what": "The self-test depends on the caller's git configuration. It lists its fixture repo with `git ls-files --others --exclude-standard`, and that also applies the running user's global excludes (core.excludesFile, which defaults to ~/.config/git/ignore) and the system config. `target/` is a common global-ignore entry on Rust machines. With it there, the untracked must-count fixture good/src/target/mod.rs is hidden, and the self-test prints '\u274c self-test: an allow above the baseline passed'. The whole check, and so `make enforce`, then exits 1 with 'The check cannot verify its own detection', on a tree that passes. Reproduced at the tip: with a HOME whose .config/git/ignore holds `target/`, the run gives rc=1. The same HOME holding `node_modules` gives rc=0. The new `unset $(git rev-parse --local-env-vars)` line does not help: it clears GIT_CONFIG_COUNT and similar variables, not HOME or XDG. The check passes today on Matt's machine (his ignore file holds only **/.claude/settings.local.json) and in CI only because neither has such an entry.",
     "fix": "Keep user and system git config away from the fixture for as long as selftest runs. For example, at the top of selftest(), after mktemp: `local HOME=\"$dir\" XDG_CONFIG_HOME=\"$dir\" GIT_CONFIG_NOSYSTEM=1; export HOME XDG_CONFIG_HOME GIT_CONFIG_NOSYSTEM`. I checked this on a copy: the self-test passes under a global `target/` ignore, and HOME is restored for the real check. GIT_CONFIG_GLOBAL=/dev/null alone is not enough, because git reads the default excludes file from XDG/HOME even when no global config names one. Say in the header that the fixture ignores the caller's git config. Re-run the self-test with a global `target/` ignore and quote the result."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:255-371 (fixtures and steps); counter at :62 and :66; compare at :155",
     "what": "Three documented behaviours have no guard. Each mutation below passes `--self-test` and also passes the real check at the tip, although it changes what the tip counts. (a) At :66, `grep { !m{^\\s*//} }` became `grep { !m{//} }`, so any line containing // is dropped: SURVIVED. At the tip this silently drops 26 lints (core 76\u219266, axum 28\u219224, src-tauri 22\u219220, hf 3\u21921, mcp, download, integration-tests). That is because this codebase usually writes `#[allow(x)] // why` (e.g. crates/gglib-axum/tests/common/harness.rs:148 and crates/gglib-core/src/paths/private.rs:114). No fixture shows that an attribute line with a trailing // still counts. (b) At :62, `$bare += $n if $n == @items;` became `$bare += 1 \u2026`, so the bare count counts attributes instead of lints: SURVIVED. Every reason-less fixture attribute names exactly one lint, so nothing tells the two apart. At the tip, bare drops from 182 to 164 (core 75\u219262, runtime 17\u219212, app-services 16\u219215); for example, crates/gglib-core/src/paths/private.rs:126 names two lints and no reason. (c) At :155, dropping `[ -z \"${5:-}\" ] &&` makes --update refuse to raise the first number: SURVIVED. No step has --update raise a lints number, yet the header (:19-21), scripts/README.md and the failure message (:426-429) all tell the user to raise it with --update.",
     "fix": "Add fixtures and steps that kill each mutation. (a) In good/src/lib.rs, add an attribute with a trailing comment, e.g. `#[allow(clippy::mu)] // a trailing comment`, and a reason containing //, e.g. `reason = \"see https://example.com\"`; both must count. (b) Make one reason-less fixture attribute name two lints, e.g. `#[allow(clippy::alpha, clippy::alpha2)]`. (c) Add a step that writes the good row's first number below its count (e.g. `good 10 <bare>`), runs `check \"$dir\" \"$dir/baseline\" update`, asserts rc 0, and asserts that the baseline row now holds the exact count. Update the expected rows, the \u2713 line, scripts/README.md and the e92fa547 message's fixture counts (\"6 lints, 3 without a reason\", \"5 lints and 2\", \"Thirty-three mutations\") to match. Re-run the three mutations and quote them as KILLED. Optional: at :70, a cfg_attr holding two allows kills `while`\u2192`if`, which also survives today (no tip file has one)."
    },
    {
     "where": "crates/gglib-proxy/src/lib.rs:139-143 (on `pub(crate) mod token_calibration;`); crates/gglib-proxy/src/loop_guard_step_trips_tests.rs:69, :91, :134; commit 8d0ef25c message (\"\u2026 except fourteen. Eight allow significant_drop_tightening in residency and proxy lock code, where a guard's scope is its critical section and the lint is never applied.\")",
     "what": "Eight attributes carry the reason \"a guard's scope is its critical section, so this lint is never applied in admission, residency or proxy lock code\". Four of them suppress only guards that tests hold for their assertions, not lock code. The token_calibration one on the mod item covers exactly one finding on all three targets: token_calibration.rs:367, inside `#[cfg(test)] mod tests` (`let guard = cal.session_snapshots.lock().unwrap(); assert!(\u2026)`). The three test functions hold a test Recorder's Mutex guard across assertions (e.g. `let scans = sink.scans.lock().unwrap();`). Neither is a critical section, so the reason is untrue for these four. So is the commit message's sentence, which counts them as residency and proxy lock code. The other four fit: sampling_audit.rs:770 (set_model_sampling resets `published` under the model_sampling guard), runtime residency, the runtime proxy supervisor, and the proxy test fixture's simulated admission slot (tests/fixtures/common.rs:633).",
     "fix": "Give those four attributes the grandfathered reason (\"grandfathered at lint inheritance, #1157\") or a reason that is true of a guard held for a test's assertions. Correct 8d0ef25c's message to match: the special-reason count (fourteen \u2192 ten, if all four move to #1157) and the significant_drop_tightening sentence (eight \u2192 four, naming them). The 212 attributes and 309 lints are unaffected."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:22-24; scripts/README.md:138-140; commit e92fa547 message (\"so a new allow needs a reason and, unless it replaces another, a raised first number in the same diff\")",
     "what": "\"so a new allow carries a reason\" (header and README) and \"a new allow needs a reason\" (message) are false. The rule only keeps a member's reason-less count from growing. A new reason-less allow therefore passes whenever it replaces another reason-less one, or whenever the row sits below its baseline, which :26 says passes until --update. Reproduced at the tip: deleting `#[allow(clippy::float_cmp)] // \u2026` at crates/gglib-core/src/services/model_import.rs:307 and adding a bare `#[allow(clippy::pedantic)]` (a whole group, no reason) to crates/gglib-core/src/lib.rs gives '\u2705 every member inherits the workspace lints, and no row allows more than its baseline', rc=0.",
     "fix": "State what the rule guarantees, e.g. \"so the number of lints allowed without a reason can only fall\", in the header, scripts/README.md and the e92fa547 message. Tell the publisher that the plan's PR subject (\"an allow cannot be added without a reason\") overclaims in the same way, and that the PR body should state the no-growth rule instead."
    }
   ],
   "commands": "VERDICT: FAIL. Defects are listed above.\n\nAll runs were at 63638c4e with dirty=0, in my worktree .../review/C3-build-r3/gglib and a second worktree of my own, gglib-mut, used for mutations. Both are removed now; the target is kept.\nCARGO_TARGET_DIR=.../targets/rv-gglib-proxy, CARGO_PROFILE_DEV_DEBUG=line-tables-only, CARGO_INCREMENTAL=0.\n\n**Earlier-round defects: all three verified fixed**\n- **Defect 1 (file listing).** Simulated read-only against the main checkout, with GIT_OPTIONAL_LOCKS=0 and a copy of the script with ROOT_DIR=~/.local/src/gglib; the main checkout's index was untouched. The .claude/ tree holds 977 .rs files, the same number the message gives. The run showed no (outside-members) failure and no \"counted in\" lines. The only \u274c lines were the eight expected `declares [lints.rustdoc]`.\n- **Defect 2 (fixtures).** The fixture .gitignore is `/target`, and src/target/mod.rs is counted. This is the fixture that D1 breaks under a global `target/` ignore.\n- **Defect 3 (workspace = false, empty table, END block).** Mutations Y1, Y2 and Y8 are KILLED.\n\n**Script**\n- `./scripts/check_lint_inheritance.sh`: rc=0.\n- `--self-test`: rc=0.\n- Under /bin/bash 3.2: rc=0.\n- `--bogus`: rc=2.\n- shellcheck 0.11: clean.\n- `--update`: a no-op against the committed baseline. Totals are 18 members, 496 lints, 182 bare; (outside-members) is 0 0; crates/build_common.rs is the one outside file.\n- 1018 .rs files are listed.\n- An independent Python counter gives the same per-member numbers as the baseline.\n- The temp dir is cleaned up on success.\n- The `unset $(git rev-parse --local-env-vars)` claim holds. I set up a real linked-worktree pre-commit hook: it exports GIT_DIR and an absolute GIT_INDEX_FILE. With the line removed, the self-test wrote gone.rs, lib.rs and merge.rs (stages 1-3) into the caller's index. With the line in place, the index stayed clean.\n\n**Wiring**\n- ci.yml enforcement has a step `./scripts/check_lint_inheritance.sh`.\n- The Makefile's enforce target has the line.\n- `make enforce`: rc=0, 0 \u274c lines.\n- CONTRIBUTING's table and both tables in scripts/README.md name the script.\n\n**Manifests**\n- All 18 members hold exactly `[lints]` with `workspace = true`.\n- The root manifest gains `missing_const_for_fn = \"allow\"`.\n- `#[allow(unsafe_code, reason = \"dlopen of gtk-layer-shell; the FFI is the module\")]` sits on the linux-only `mod layer_shell;`.\n\n**Clippy, with `cargo clippy --workspace --all-targets --all-features -- -D warnings`**\nI ran it twice: once as-is, and once with `-D warnings -D warnings` to force every workspace unit to be linted again. All 18 members were checked each time (17 \"Compiling\" because they have build scripts, 1 \"Checking\").\n- aarch64-apple-darwin, native: rc=0.\n- x86_64-unknown-linux-gnu, a check-only cross build from macOS with zig cc and a fake pkg-config: rc=0.\n- x86_64-pc-windows-gnu, the same kind of cross check: rc=0.\n- Only CI can run: ubuntu natively with the real gtk/webkit/ayatana libraries, and windows-2022 x86_64-pc-windows-msvc (no `target_env` cfg exists in the tree, so gnu is a close proxy). macos-14 is the same target as mine.\n\n**Other gates**\n- `cargo test --workspace --no-fail-fast`: rc=0, 4113 passed, 0 failed, 70 ignored, 111 test result lines.\n- `make bindings-check`: rc=0, 199 types.\n- `make doc-check` (RUSTDOCFLAGS=-D warnings): rc=0.\n- `cargo fmt --all -- --check`: rc=0.\n- `check_boundaries.sh`: rc=0.\n- `check_rust_complexity.sh`: rc=0. rust-complexity-baseline.txt is unchanged by the range.\n- `cargo check --workspace --all-targets --all-features` at ca7e3b81: rc=0, so commit 1 builds on its own.\n\n**Allows**\n- 213 added attributes: 198 carry the #1157 reason, 14 carry specific reasons, plus the one unsafe_code allow.\n- All have a reason, all are `allow` (none is `expect`), and none is crate-level.\n- Commit 2 has 212 attributes naming 309 lints, on 142 fns, 54 mods, 7 structs, 1 static and 1 use, as the message says.\n- No added attribute repeats a lint that an enclosing mod declaration or a crate-level allow already allows. I checked this with a parser over the whole module tree.\n- Of the 60 mod-level attributes, 44 sit on at-cap files. The other 16 have too little room for all their attributes (headroom 1-13 lines against 4-5+ per attribute). The one exception is layer_shell, which the plan places.\n- The cast reason is right: u64::MAX ms is about 584.9 million years.\n- The tray `sync` in build.rs is cfg(not(linux)), and the Linux `sync` awaits.\n- setup_app: main.rs was at its cap of 303 and is now 301. The body has no `?`. The commit's deletions are only in setup_app.\n\n**The --fix (commit 1), read by sample**\n- All five if_not_else swaps, including the question_input stdin branch, the remote print_status branch with its comment moved correctly, cache_lifecycle, chat_completions streaming, and the health check.\n- redundant_else in ports.rs.\n- map_unwrap_or: map_or and map_or_else keep eager and lazy evaluation as they were.\n- redundant_closure_for_method_calls, including char::is_ascii_digit on &char, String::as_str, Path::to_path_buf, and PoisonError::into_inner.\n- bool_to_int_with_if: i64::from(bool).\n- implicit_clone: to_path_buf became clone on &PathBuf.\n- All 28 duration_suboptimal_units rewrites keep their values.\n- README edits are backticks only. TS binding changes are JSDoc only.\n- The 37 poison-recovery closures remain as closures (2+18+6+2+9 across the five files).\n- No behaviour change found. No needless_borrow rewrites exist in the range.\n\n**K7**\n- The old comments were 36 and 9 lines, as the message says.\n- The new text is true: unreachable_pub fires only on effectively non-public items, and dead_code already judges by that. redundant_pub_crate is nursery and fires on pub(crate) in a private module.\n- The diff is comment lines only.\n\n**Commit metadata**\n- Attribution grep: 0.\n- Author and committer on all four commits: Matt O'Grady <sewing.wader8c@icloud.com>.\n- The subjects are conventional, lowercase and declarative.\n\n**Non-blocking notes (not required for PASS)**\n1. The counter fails open. If perl dies on a file, `read` gets empty values, `[ \"\" -gt N ]` evaluates false, and the row passes. Mutation X23 (`|| echo \"0 0\"`) survives, which amounts to the same thing. Consider an integer guard on lints and bare.\n2. \"Files already at their size cap keep their size\" (ca7e3b81): 14 at-cap files shrank, e.g. crates/gglib-axum/tests/integration_routes.rs went 1029\u21921015. \"None grows\" is the true wording.\n3. ca7e3b81 names only src-tauri as newly denying unsafe code. gglib-proxy also does now, since its old [lints.rust] held only unreachable_pub.\n4. docs/adr/0008-two-binaries-one-daemon.md:45-57 (finding 1) says unsafe_code is re-declared in gglib-cli's manifest and that src-tauri \"is not under the deny at all\". Both stop being true with this PR. The ADR is frozen; CONTRIBUTING's way to record this is a log-0008.md entry (\"a follow-up that landed\").\n5. #1157's body still describes counting `allow(clippy::` occurrences. That is for the publisher.\n6. The script does not watch the root [workspace.lints] or .cargo rustflags, and a group allow such as clippy::pedantic counts as one lint. This is within the plan; I am only flagging it.\n7. 63638c4e says \"most of them correcting earlier wordings of themselves\". About 19 of the 45 removed lines are self-corrections; \"many\" would be safer.\n8. Two leaked self-test temp dirs from about 15:56-15:59 were found in $TMPDIR. The RETURN trap does not fire when `--self-test` exits through set -e. This is trivial.",
   "mutations": "Script mutations ran as `bash <mutated copy> --self-test` from gglib-mut. The unmutated script gave rc=0 before and after.\n\n**Killed**\nThe failing self-test assertion is in brackets.\n- X1: [lints] keys never inspected [six bad manifests].\n- X13: inner #![\u2026] ignored [allow above baseline].\n- X15: a manifest with 0 [lints] tables accepted [six].\n- X17: --update writes even on failure [--update raised].\n- X21: a member missing its Cargo.toml ignored [six].\n- X22: the outside row never compared [outside allow passed].\n- Y1: the END block's inherit branch never fires [six].\n- Y2: the END block emptied [six].\n- Y3: --others dropped [allow above baseline].\n- Y4: --exclude-standard dropped [six].\n- Y5: --deduplicate dropped [six].\n- Y6: a stale baseline row accepted [stale row].\n- Y7: the zero-scan guard removed [empty listing].\n- Y8: `workspace = false` accepted [six].\n- Y9: lints no-growth removed [allow above baseline].\n- Y10: bare no-growth removed [outside allow passed].\n- Y11: `reason` counted as a lint [six].\n- Gate: a failing self-test makes the real run exit 1 with \"cannot verify its own detection\".\n\n**Survived (defect D2)**\nEach of these also passes the real check at the tip.\n- X10, dropping any line containing //: the tip loses 26 lints.\n- X2, bare counting attributes instead of lints: the tip's bare count falls from 182 to 164.\n- X19, --update refusing to raise lints.\n\n**Survived (not self-testable, or no tip instance)**\n- X12, `while`\u2192`if` in cfg_attr: no tip file has a cfg_attr holding two allows.\n- X20, the unset line removed: I verified it separately in a real linked-worktree hook.\n- X23, count failure turned into \"0 0\": equivalent to non-blocking note 1.\n\n**Environment mutation (defect D1)**\n- HOME with .config/git/ignore = `target/`: the self-test FAILED, and the real check gave rc=1 on a passing tree.\n- `node_modules` in the same place: rc=0.\n- My hermetic-selftest patch on a copy: rc=0 under `target/`.\n\n**Real-tree mutations, run against the unmutated script at the tip; each file restored afterwards and git status clean**\n- T1, proxy [lints]\u2192[lints.clippy]: rc=1, \"declares [lints.clippy]\".\n- T2, a `rust = {\u2026}` key beside workspace = true: rc=1, \"[lints] holds `rust = \u2026`\".\n- T3, runtime `workspace = false`: rc=1.\n- T3b, the root inline `lints = { workspace = true }`: rc=1, \"has 0 [lints] tables\". This is a strict false positive, acceptable.\n- T4, src-tauri with no [lints]: rc=1.\n- T5, an extra [lints.rust] table: rc=1.\n- T6, a bare significant_drop_tightening allow in runtime admission/lease.rs: rc=1, \"18 \u2026 without a reason, baseline 17\".\n- T6b, the same with a reason: rc=1, \"91 \u2026 baseline 90\".\n- T7, a reasoned allow in the proxy: rc=1, 78>77.\n- T8, `#![expect(\u2026, reason)]` in an axum tests/ crate root: rc=1, 29>28.\n- T9, `cfg_attr(windows, allow(unsafe_code))` in src-tauri: rc=1, bare 5>4.\n- T10, an untracked new core file with an allow: rc=1.\n- T11, an untracked crates/zz_stray.rs: rc=1, \"(outside-members) 1 \u2026 baseline 0\".\n- T12, a bare-for-bare swap in core: rc=0. This is defect D4."
  }
 ],
 "wf_92898c52-313": [
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "base": "tmp/c3-base",
   "head_sha": "591b698ceebea4adb11640db28ac51ce2fdd0919",
   "commits": [
    "ca7e3b81 chore(lints): every crate inherits the workspace lints (unchanged)",
    "98f37d3c chore(lints): the remaining findings are grandfathered where they stand (was 8d0ef25c; fixup + reworded body)",
    "1f694177 chore(lints): enforcement checks lint inheritance and each crate's allow count (was e92fa547; fixup + reworded body)",
    "591b698c chore(lints): the visibility lints' comments state the rule (was 63638c4e; tree diff and raw message byte-identical to before)"
   ],
   "summary": "I fixed all four defects and pushed nothing. Each fix is folded into the commit it belongs to, with a fixup and an autosquash rebase. The subjects are unchanged. The bodies of 98f37d3c and 1f694177 were rewritten from files and compared byte for byte against those files after the rebase. I checked the replay: the tip tree equals the pre-rebase fixup tip, and the tip's diff against 63638c4e is exactly the fix diff.\n\nFiles changed against 63638c4e:\n- crates/gglib-proxy/src/lib.rs\n- crates/gglib-proxy/src/loop_guard_step_trips_tests.rs\n- scripts/check_lint_inheritance.sh\n- scripts/README.md\n\nscripts/lint-allow-baseline.txt is unchanged. Running --update at the tip gives no diff, and the baseline still totals 496 lints, 182 of them without a reason.\n\n**Self-test isolation (defect 1).** selftest() now sets `local XDG_CONFIG_HOME=\"$dir\" GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1` and exports them. The fixture repo is created with `git init -q --template=`.\n- At 63638c4e, five settings each failed the self-test with \"\u274c self-test: an allow above the baseline passed\": a HOME whose .config/git/ignore holds `target/`, an XDG_CONFIG_HOME git/ignore, GIT_CONFIG_GLOBAL or GIT_CONFIG_SYSTEM naming such an excludes file, and a GIT_TEMPLATE_DIR whose info/exclude holds `target/`.\n- At the tip, `env -u XDG_CONFIG_HOME HOME=<dir whose .config/git/ignore holds target/> ./scripts/check_lint_inheritance.sh --self-test` prints the new \u2713 line with rc=0. It also passes with all five settings at once.\n- The real check still reads the caller's git config, because the variables are local. I put an untracked `crates/gglib-proxy/src/target/probe.rs` holding `#[allow(clippy::probe)]` in the tree. With plain config the check fails with \"crates/gglib-proxy: 10 lints allowed without a reason, baseline 9\". With the caller's XDG excludes holding `target/` it passes. I then removed the probe.\n\n**Counter fixtures (defect 2).** good/src/lib.rs now has:\n- `#[allow(clippy::alpha, clippy::alpha2)]`\n- `#[allow(clippy::mu)] // a trailing comment`\n- `#[allow(clippy::nu, reason = \"see https://example.com\")]`\n- a cfg_attr holding `allow(clippy::delta)` and `expect(clippy::delta2, reason = \"known\")`\n\nThat file now counts 10 lints, 5 without a reason, and the good row is 15 7. A new step sets the good row's first number to 14, runs the update path, and asserts rc 0 and `^good 15 7$`. The other expected rows, the \u2713 line, scripts/README.md and the commit body are updated to match.\n\n**Lock-code reasons (defect 3).** The three test attributes in loop_guard_step_trips_tests.rs now carry the grandfathered reason. On the token_calibration mod item, significant_drop_tightening moved into the existing grandfathered attribute. That makes 211 attributes naming 309 lints, recounted from the new commit's diff, with ten special reasons. The 98f37d3c body now names the four remaining lock-code allows.\n\n**Wording (defect 4).** The header, scripts/README.md and the 1f694177 body now say that --update raises the first number but refuses to raise the second, so the baseline's number of lints allowed without a reason can only fall.",
   "commands_run": "- Before the fix, with the special significant_drop_tightening allows stripped temporarily and then restored with git checkout and touch: `cargo clippy -p gglib-proxy -p gglib-runtime --all-targets --all-features --message-format=short` located the 13 findings: loop_guard_step_trips_tests.rs:78/97/98/139, token_calibration.rs:367 (in its tests module), sampling_audit.rs:770, tests/fixtures/common.rs:633, residency/launch.rs:174/285, residency/mod.rs:610, supervisor.rs:312/463/512.\n- Defect 1 reproduced at 63638c4e: `--self-test` under HOME, XDG_CONFIG_HOME, GIT_CONFIG_GLOBAL, GIT_CONFIG_SYSTEM or GIT_TEMPLATE_DIR set to a `target/`-ignoring setup failed each time with rc=1. `GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=<hostile>` passed, which shows NOSYSTEM overrides it.\n- `cargo fmt --all` then `cargo fmt --all --check`: rc 0.\n- `cargo clippy -p gglib-proxy --all-targets --all-features -- -D warnings`: rc 0, before and after the rebase.\n- `cargo test -p gglib-proxy --lib -- loop_guard_step_trips token_calibration`: 12 passed, 0 failed.\n- `./scripts/check_lint_inheritance.sh` at the tip: \u2713 self-test, then \u2705 with rc 0. `--update` left no baseline diff.\n- `./scripts/check_lint_inheritance.sh` at 1f694177, in a detached temporary worktree that has since been removed: \u2713 then \u2705, rc 0. The proxy and runtime sources at 98f37d3c are identical to the tip's.\n- `./scripts/check_rust_complexity.sh`: \u2705. `./scripts/check_boundaries.sh`: rc 0. `shellcheck scripts/check_lint_inheritance.sh`: rc 0. `bash -n`: ok.\n- Mutation runner scratchpad/c3fix2/run.py (the prior 33 mutations plus 8 new), log in scratchpad/c3fix2/mutations.log, script sha ee332f17 (the tip's):\n  - plain env: killed=37 survived=4 of 41; the four survivors are the isolation mutations, which is expected with no hostile config;\n  - hostile env (HOME with .config/git/ignore `target/`, GIT_CONFIG_GLOBAL and GIT_CONFIG_SYSTEM naming an excludesFile holding `target/`, GIT_TEMPLATE_DIR whose info/exclude holds `target/`): killed=41 survived=0 of 41; the unmutated script passed before and after.\n- Recounted 98f37d3c's added attributes with scratchpad/c3-attrs.pl: 211 attributes, 309 lints; 4 lock-code, 1 cast, 5 Tauri-shape, 201 grandfathered.\n- `git log --format=%B tmp/c3-base..HEAD | grep -ciE 'co-authored|claude-session|signed-off-by|generated with|\ud83e\udd16'` printed 0. Author and committer on all four commits are Matt O'Grady <sewing.wader8c@icloud.com>, unsigned.\n- Raw messages compared with `git cat-file commit | sed '1,/^$/d' | cmp`: both reworded bodies match their files, and the tip message is unchanged.",
   "mutations": "Each mutation is run on a copy of the tip script with `bash m.sh --self-test`, in the hostile env.\n- (a) comment filter `grep { !m{^\\s*//} }` changed to `grep { !m{//} }`: KILLED, \"an allow above the baseline passed\".\n- (b) `$bare += $n if $n == @items` changed to `$bare += 1 if $n == @items`: KILLED, \"--update raised the count of allows without a reason\". The good row counts 6 instead of 7, so that step's expected text is missing.\n- (c) `[ -z \"${5:-}\" ] && [ \"$2\" -gt \"$base_lints\" ]` changed to `[ \"$2\" -gt \"$base_lints\" ]`: KILLED, \"--update did not raise the count of allows\".\n- (optional) cfg_attr `while` changed to `if`: KILLED, \"an allow above the baseline passed\".\n- Dropping XDG_CONFIG_HOME, dropping GIT_CONFIG_GLOBAL, dropping GIT_CONFIG_NOSYSTEM, and dropping `--template=` (four separate mutations): each KILLED in the hostile env and SURVIVED in the plain env, as expected.\n- All 33 earlier mutations are still KILLED in both envs.\n- Not a mutation: removing `local` from the isolation line is not detectable by --self-test. I checked the local scoping by hand with the probe file described in the summary.",
   "deviations": "- Defect 1: fixed, but not with HOME. HOME alone would not stop a caller's GIT_CONFIG_GLOBAL or XDG_CONFIG_HOME, and both failed the self-test when I tried them. With XDG_CONFIG_HOME and GIT_CONFIG_GLOBAL isolated, HOME changes nothing on git 2.32 or later, so a mutation removing it would survive. I isolate XDG_CONFIG_HOME, GIT_CONFIG_GLOBAL=/dev/null and GIT_CONFIG_NOSYSTEM=1 instead; removing any one of them fails the self-test in the hostile env. I also added `init --template=`, because GIT_TEMPLATE_DIR's info/exclude caused the same failure. The header says the fixture ignores the caller's global and system git config.\n- Defect 2: fixed. (a), (b) and (c) are KILLED, and I added the optional cfg_attr fixture, which kills the while-to-if mutation. The expected rows, \u2713 line, scripts/README.md and the 1f694177 body are updated; the mutation count went from thirty-three to forty-one. The body says the four isolation mutations fail only when the caller's excludes files or init template ignore `target/`.\n- Defect 3: fixed. The four allows (token_calibration and three in loop_guard_step_trips_tests) are grandfathered. Because the token_calibration mod item already had a grandfathered attribute, I merged the lint into it rather than keep two attributes with the same reason, so the 98f37d3c body now says 211 attributes, not 212; lints stay at 309. Special reasons go from fourteen to ten, and lock-code allows from eight to four, named in the body: runtime residency, runtime proxy supervisor, proxy sampling_audit, and the proxy tests' fixtures/common. The reviewer did not flag that last one, and I kept its special reason. Its finding is at tests/fixtures/common.rs:633: ResidentSimRuntime::admit takes the slot lock in its own block, the way the real queue does. It is not a guard held across assertions. The baseline is unchanged: --update gives no diff.\n- Defect 4: fixed, and narrowed more than suggested. A count below its baseline passes, so without --update the actual count can climb back up to it. Only the baseline's number is sure to fall. So the header says the number \"can only fall\" about the baseline, and the README and the 1f694177 body say \"the baseline's number of lints allowed without a reason can only fall\". Two more sentences had the same flaw and are also fixed: the header's `lints` line (\"a new allow means fixing an old one, or raising this number\") and the README's \"so a deliberate allow shows up as a number going up in the diff\".\n- For the orchestrator: the PR title \"\u2026the lints allowed without a reason can only fall\" is true of the baseline number, not of the live count, as described above. The 1f694177 subject is 80 characters; I kept it as instructed.",
   "review_focus": "- scripts/check_lint_inheritance.sh around lines 232-234: the isolation relies on bash `local` together with `export` inside selftest() and must not reach the real check. The probe test confirmed this.\n- On git older than 2.32, GIT_CONFIG_GLOBAL is ignored, so ~/.gitconfig's core.excludesFile would still be read there. CI's git is newer.\n- The 1f694177 body sentence \"Those four fail it only when the caller's excludes files or init template ignore `target/`\".\n- Whether the fixtures/common allow in crates/gglib-proxy/tests/fixtures/mod.rs:3-7, a simulated admit in test code, should keep the lock-code reason.\n- The merged attribute on `pub(crate) mod token_calibration` in crates/gglib-proxy/src/lib.rs.\n- Mutation log: /private/tmp/claude-501/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6/scratchpad/c3fix2/mutations.log"
  },
  {
   "verdict": "FAIL",
   "defects": [
    {
     "where": "scripts/check_lint_inheritance.sh:19-23 (header, added by 1f694177; the wording is repair 4's)",
     "what": "The `bare` entry reads: \"the lints in an attribute with no `reason = \"\u2026\"`. It may not grow, and `--update` refuses to raise it, so this number can only fall.\" The entry defines `bare` as the counted lints, so \"this number\" reads most naturally as the live count, and the live count can rise. compare() at :152 fails only when count > baseline, so a count that fell without `--update` can climb back up to the baseline. I showed this at the tip. Removing the bare `#[allow(clippy::result_large_err)]` at crates/gglib-proxy/src/mcp/handlers.rs:376 took proxy's bare count from 9 to 8, and the check passed (rc 0). Adding an untracked crates/gglib-proxy/src/zz_probe/mod.rs holding `#[allow(clippy::probe)]` took it back to 9, and the check passed again (rc 0). Only the baseline's second number is monotone. README:138-140 and the 1f694177 body now say so explicitly ('in the baseline', 'the baseline's number'), but the header still does not, even though the author's report says it does. The `lints` entry has the same shifting referent: in \"It may not grow; `--update` sets it to the count\", the first \"it\" is the count and the second is the baseline number. The round asked for the header to state only the no-growth guarantee. Under its natural reading, this sentence claims the count can only fall, which is false at this commit.",
     "fix": "Fold a fixup into 1f694177 that changes only these five header lines, so each names the count and the baseline explicitly, e.g.\n#      lints    every lint so named. The count may not exceed the baseline's\n#               first number; `--update` sets that number to the count.\n#      bare     the lints in an attribute with no `reason = \"\u2026\"`. The count\n#               may not exceed the baseline's second number, and `--update`\n#               refuses to raise it, so that number can only fall.\nChange nothing else. Read the header back at the new hash, and rerun `--self-test` and `bash -n`."
    },
    {
     "where": "Plan PR title for C3 (the orchestrator owns it; it is not in the commit range, but it becomes the squash subject on main)",
     "what": "The title is \"chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason can only fall\". It has the same flaw as defect 1. The number of lints allowed without a reason can rise back up to its baseline, as shown above; only the recorded baseline number can only fall. The author flagged this for the orchestrator. It is listed here so it is not lost. It does not block the push of the commits.",
     "fix": "The orchestrator retitles at PR creation to a no-growth statement, e.g. \"chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason may not grow\", or else names the baseline explicitly. The author changes nothing."
    }
   ],
   "commands": "All runs were in my own detached worktree at 591b698ceebea4adb11640db28ac51ce2fdd0919 (since removed). CARGO_TARGET_DIR=.../targets/rv-gglib-proxy, CARGO_PROFILE_DEV_DEBUG=line-tables-only, CARGO_INCREMENTAL=0. web_ui was copied in and node_modules symlinked. Every log's first line is the hash, and dirty=0.\n\n**Verdict in brief.** Repairs 1, 2 and 3 are verified. Repair 4 is verified for README.md and all four commit messages. It is not verified for the script header (defect 1). Everything else passes, so the fix is one five-line header fixup plus an orchestrator retitle.\n\n**Workspace build and gates**\n- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: RC=0, all 18 members rechecked.\n- `make enforce`: RC=0, zero \u274c. check_lint_inheritance printed \u2713 self-test then \u2705. check_rust_complexity \u2705, check_ts_bindings \u2705, check_readme_tables \u2705, check_adrs \u2705.\n- `./scripts/check_boundaries.sh`: rc 0.\n- `cargo fmt --all --check`: 0. `shellcheck` 0.11.0: 0. `bash -n`: 0.\n- `RUSTDOCFLAGS=-D warnings cargo doc -p gglib-proxy --no-deps --document-private-items`: 0.\n- `cargo test -p gglib-proxy --lib -- loop_guard_step::trips_tests token_calibration`: 17 passed, 0 failed. The three re-reasoned trips_tests and token_calibration::tests::session_snapshots_are_bounded_by_max_session_snapshots ran by name.\n- `./scripts/check_lint_inheritance.sh --update`: no baseline diff. The baseline sums to 496 lints, 182 without a reason.\n- Cross-target clippy (`--target x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu`) could not run: the ring and aws-lc-sys build scripts need a cross C toolchain. The repair only moves or re-reasons `allow` attributes on the same items, with no cfg involved, so the platforms cannot differ on it.\n\n**Repair 1 (self-test isolation).** I ran `--self-test` under these settings, with XDG_CONFIG_HOME, GIT_CONFIG_* and GIT_TEMPLATE_DIR cleared first:\n- plain\n- HOME whose .config/git/ignore holds `target/`\n- XDG_CONFIG_HOME git/ignore\n- HOME/.gitconfig with core.excludesFile\n- GIT_CONFIG_GLOBAL naming an excludesFile\n- GIT_CONFIG_SYSTEM naming an excludesFile\n- GIT_TEMPLATE_DIR with info/exclude\n- global init.templateDir\n- all at once\n\nResults:\n- At the tip: rc 0 in every setting, with bash 5.3 and with /bin/bash 3.2.\n- At 63638c4e: rc 1 in every setting except plain (\"an allow above the baseline passed\"). This reproduces the original defect.\n- GIT_CONFIG_COUNT/KEY/VALUE and GIT_CONFIG_PARAMETERS are both unset by `--local-env-vars`, and the self-test passes with either set.\n\nThe real check still reads the caller's config. I put an untracked crates/gglib-proxy/src/target/probe.rs holding `#[allow(clippy::probe)]` in the tree and ran the full script:\n- plain env: fails with \"crates/gglib-proxy: 10 lints allowed without a reason, baseline 9\".\n- HOME-xdg, XDG_CONFIG_HOME, HOME/.gitconfig, GIT_CONFIG_GLOBAL or GIT_CONFIG_SYSTEM excludes: passes (\u2705).\n- GIT_CONFIG_NOSYSTEM=1 with GIT_CONFIG_SYSTEM set: fails.\n\nSo each variable is restored after selftest(), whether the caller had it set or unset. The same results hold under /bin/bash 3.2. No mktemp directory leaked.\n\n**Repair 3 and the counts in 98f37d3c.**\n- I parsed `git diff ca7e3b81 98f37d3c` myself: 211 attributes naming 309 lints, on 205 items (142 fn, 54 mod, 7 struct, 1 static, 1 use). 201 attributes carry the grandfathered reason and 10 carry a special one: 4 lock-code, 1 cast, 5 Tauri. There is no `#![allow]`. The only added non-attribute lines are the two setup_app lines.\n- To find the findings, I swapped every significant_drop_tightening allow in the proxy and runtime for clippy::zst_offset, then restored the files with `git checkout` and `touch`. Clippy located: loop_guard_step_trips_tests.rs:82/105/106/151, token_calibration.rs:367 (in its tests module), sampling_audit.rs:770 (set_model_sampling), tests/fixtures/common.rs:633 (ResidentSimRuntime::admit's slot lock), residency/launch.rs:174/285, residency/mod.rs:610, supervisor.rs:312/463/512 (all non-test lock code, before each file's cfg(test)), and logs.rs:102 (grandfathered).\n- The four lock-code reasons are true. The body's description of them is accurate.\n- token_calibration.rs is at its cap (370 = its rust-complexity baseline), so the allow belongs on the mod item. No inner allow duplicates it.\n- Proxy and runtime clippy with -D warnings was clean again after the restore.\n\n**Repair 4 and history checks.**\n- `git range-diff tmp/c3-base 63638c4e 591b698c`: ca7e3b81 and the tip commit are unchanged. The other two differ only in the intended hunks and message bodies.\n- `git diff 63638c4e 591b698c` is byte-identical to the author's c3fix2/fix.diff.\n- Both reworded commit bodies are byte-identical to msg-8d0.txt and msg-e92.txt, and the tip's message is byte-identical to 63638c4e's.\n- Attribution grep: 0. Pre-push `-by:` grep: 0. Author and committer on all four commits: Matt O'Grady <sewing.wader8c@icloud.com>, unsigned.\n- Subject lengths are 54, 71, 78 and 59 characters.\n- A grep for the old per-allow wording (\"new allow\", \"carries a reason\", \"deliberate allow\", \"fixing an old one\", \"review sees\") found none in scripts/, CONTRIBUTING, the Makefile, ci.yml or the commit messages.\n\n**Notes (no change needed)**\n- The fixtures/common.rs allow keeping the lock-code reason is acceptable: it is a simulated admission check-and-set whose guard scope is its critical section.\n- On git 2.31.x, GIT_CONFIG_GLOBAL is ignored. The script already needs \u22652.31 for `--deduplicate`, and CI's git is newer.",
   "mutations": "Script: my runner at scratchpad/review/C3-build-r1/mut/run.py, on copies of the tip script (sha256 02ea718698fd), running `bash m.sh --self-test`. Two environments: plain, and hostile (HOME .config/git/ignore, XDG git/ignore, GIT_CONFIG_GLOBAL and GIT_CONFIG_SYSTEM excludesFile, and GIT_TEMPLATE_DIR info/exclude, each holding `target/`). The unmutated script passed in both environments, before and after the runs.\n\n**The three named mutations.** Each is killed at the tip and survived at 63638c4e.\n- (a) `grep { !m{^\\s*//} }` changed to `grep { !m{//} }`: KILLED by \"an allow above the baseline passed\", in both environments.\n- (b) `$bare += $n if` changed to `$bare += 1 if`: KILLED by \"--update raised the count of allows without a reason\", in both environments.\n- (c) `[ -z \"${5:-}\" ] &&` dropped from the lints compare: KILLED by \"--update did not raise the count of allows\", in both environments.\n\n**Extra counter mutations.** All KILLED in both environments.\n- (d) cfg_attr `while` changed to `if`: \"an allow above the baseline passed\". It survived at 63638c4e.\n- (a1) `!m{//\\s}`, which drops only the trailing-comment line: \"an allow above the baseline passed\".\n- (q) strip `//` to the end of every line: \"an allow above the baseline passed\".\n- (e) the cfg_attr inner pattern matches only `allow`: \"an allow above the baseline passed\".\n- (j) every lint counted bare: \"the first fixture did not fail on exactly the six bad manifests\".\n- (k) `reason` counted as a lint: the same message.\n- (l) strings not blanked before the split: the same message.\n- (n) --update may raise bare: \"--update raised the count of allows without a reason\".\n- (p) --update writes even on failure: the same message.\n\n**Isolation mutations.** Each is KILLED in the hostile environment (\"an allow above the baseline passed\") and SURVIVES in the plain one, which is expected and matches the 1f694177 body's wording.\n- (f) drop XDG_CONFIG_HOME\n- (g) drop GIT_CONFIG_GLOBAL\n- (h) drop GIT_CONFIG_NOSYSTEM\n- (i) drop `--template=`\n- (r) drop the `export` line (my addition)\n\n**Earlier mutations spot-checked at the tip.** All KILLED, plain environment.\n- drop `--deduplicate`: \"first fixture\u2026\".\n- drop `--exclude-standard`: \"first fixture\u2026\".\n- count only src/: \"allow above the baseline\".\n- bare growth allowed: \"an allow outside every member passed\".\n- lints growth allowed: \"allow above the baseline\".\n- outer `expect` not counted: \"allow above the baseline\".\n\nThe author's log (c3fix2/mutations.log) agrees: 37/41 killed in the plain environment, 41/41 in the hostile one.\n\n**Rust-side checks.** Removing significant_drop_tightening from the merged token_calibration attribute, and from each of the three trips_tests attributes, makes clippy fire at token_calibration.rs:367 and trips_tests.rs:82/105/106/151. The allows are load-bearing.\n\n**Wording check (defect 1).** I removed the bare allow at handlers.rs:376, and the check passed with proxy at 8/9. I then added a new bare allow in the proxy crate, and the check passed with proxy at 9/9. So the live count rose while every check passed."
  },
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "base": "tmp/c3-base",
   "head_sha": "4197b90789008b94dc2977a33ffc4ce494a7bf4a",
   "commits": [
    "ca7e3b81 chore(lints): every crate inherits the workspace lints (unchanged)",
    "98f37d3c chore(lints): the remaining findings are grandfathered where they stand (unchanged)",
    "e1c47b3f chore(lints): enforcement checks lint inheritance and each crate's allow count (was 1f694177; header lines 19-23 of the script changed, message byte-identical)",
    "4197b907 chore(lints): the visibility lints' comments state the rule (was 591b698c; tree change and message identical)"
   ],
   "summary": "I fixed defect 1 with a fixup folded into 1f694177, which is now e1c47b3f. The fixup changes only the five `lints`/`bare` header lines of /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/scripts/check_lint_inheritance.sh (lines 19-23). I used the reviewer's wording word for word, so each sentence now says whether it means the live count or the baseline's number:\n#      lints    every lint so named. The count may not exceed the baseline's\n#               first number; `--update` sets that number to the count.\n#      bare     the lints in an attribute with no `reason = \"\u2026\"`. The count\n#               may not exceed the baseline's second number, and `--update`\n#               refuses to raise it, so that number can only fall.\nRead back with `git show e1c47b3f:scripts/check_lint_inheritance.sh | sed -n 17,26p`, it shows exactly this. Nothing else changed:\n- `git range-diff tmp/c3-base 591b698c 4197b907` gives ca7e3b81 and 98f37d3c as `=`, 1f694177 \u2192 e1c47b3f differing only in this hunk, and 591b698c \u2192 4197b907 as `=`.\n- `git diff 591b698c 4197b907` is only this hunk.\n- The e1c47b3f and 4197b907 messages are byte-identical to 1f694177's and 591b698c's (cmp).\n- The worktree is clean. Nothing was pushed.\n\nDefect 2 (the PR title) belongs to the orchestrator. It is not in the commit range, so I left it, and it is repeated under deviations so it is not lost.",
   "commands_run": "All commands ran in /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib at tip 4197b907, with `git status --porcelain` empty before and after.\n\n**Fixup**\n- `git commit --no-gpg-sign --fixup=1f694177` then `GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash tmp/c3-base`: rebased cleanly.\n\n**Checks**\n- `bash -n scripts/check_lint_inheritance.sh`: rc 0.\n- `shellcheck scripts/check_lint_inheritance.sh`: rc 0.\n- `./scripts/check_lint_inheritance.sh --self-test`: rc 0 under bash 5, printing \"\u2713 self-test: \u2026 --update raises the first number, never the second\".\n- `/bin/bash scripts/check_lint_inheritance.sh --self-test` (bash 3.2): rc 0.\n- `./scripts/check_lint_inheritance.sh` (full check): rc 0, printing \"\u2705 every member inherits the workspace lints, and no row allows more than its baseline\".\n- The five new header lines are all under 80 columns (awk length check). The lines it flagged are existing code lines outside the change.\n\n**History and authorship**\n- `git log --format=%B tmp/c3-base..HEAD | grep -ciE 'co-authored|claude-session|signed-off-by|generated with|\ud83e\udd16'`: 0.\n- All four commits have Matt O'Grady <sewing.wader8c@icloud.com> as author and committer, unsigned (%G? = N).\n- Subject lengths are 54, 71, 78 and 59, unchanged.\n\n**Not rerun**\n- No cargo build, clippy, fmt or test: the change is to a shell comment only, and no Rust file changed.\n- `make enforce` was not rerun. Its other scripts do not read this header, and the lint-inheritance piece of it was run directly above.",
   "deviations": "Defect 1 (the script header at scripts/check_lint_inheritance.sh:19-23): fixed. The fixup is folded into 1f694177, now e1c47b3f, using the reviewer's wording word for word, and nothing else changed.\n\nDefect 2 (the plan's PR title, \"\u2026and the lints allowed without a reason can only fall\"): not changed by me. The reviewer assigned it to the orchestrator, and the brief forbids editing the plan. The claim is false as a statement about the live count: at 591b698c the reviewer took proxy's bare count from 8 back up to 9 with rc 0. At PR creation the orchestrator should retitle it to a no-growth statement, e.g. \"chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason may not grow\".",
   "mutations": "No new mutations. The only change is five comment lines, and no code a mutation could reach changed. I did not rerun the counter and isolation mutations: `git diff 591b698c 4197b907` shows the script's code is byte-identical outside the header, so those results still hold. The self-test that kills them was rerun and passes at the tip under bash 5 and bash 3.2.",
   "review_focus": "**Header wording.** Read lines 17-26 of scripts/check_lint_inheritance.sh at e1c47b3f.\n- Line 25, \"A count below its baseline passes, and `--update` locks it in\", is unchanged. It is consistent with the new lines, but it was out of scope.\n- \"`--update` sets that number to the count\" (lints) describes a successful run. If any rule fails, check() writes no baseline, so nothing is set (the `[ -n \"$update\" ] && [ \"$failed\" -eq 0 ]` guard just before the baseline write). The original sentence made the same claim, and the reviewer proposed keeping it, so I did not add a qualifier.\n\n**Commit message.** The unchanged e1c47b3f body still says \"Neither may grow\" about the two baseline comparisons. The reviewer accepted that as a no-growth statement, and I did not touch it."
  },
  {
   "verdict": "FAIL",
   "defects": [
    {
     "where": "scripts/check_lint_inheritance.sh:323, :346, :353, :361, :387, :396 (selftest). The failure paths it leaves unguarded are at :158, :174, :180, :203, :209, :215 and :226 (check/compare).",
     "what": "The self-test never checks check()'s exit status on a failing fixture. Every failing case is `out=\"$(check \u2026 || true)\"` followed by a grep on the \u274c text, so a mutation that keeps the message but drops the failure passes the self-test. KILLED only if the self-test fails; these SURVIVED under bash 5.3:\n- `return \"$failed\"` changed to `return 0` at :226. This kills the whole gate. On the real tree, a new bare allow in crates/gglib-db/src/zz_new.rs printed \"\u274c crates/gglib-db: 1 lints allowed without a reason, baseline 0\", then \"\u2705 every member inherits\u2026\", and exited 0.\n- compare's lints `return 1` changed to `return 0` at :158. The first number can then grow while the check passes.\n- the zero-scan `return 1` changed to `return 0` at :209.\n- dropping `failed=1` at :174 (missing manifest), :180 (bad manifest), :203 (outside-members) or :215 (stale baseline row).\nWhy it matters: line 418 says the self-test is there so the verdict means something, and the verdict is exactly what goes unchecked. The e1c47b3f message says mutations \"letting either number grow\" fail the self-test, and a growth that prints \u274c and then passes does not. Brief rule 2 calls a test that passes either way vacuous.",
     "fix": "1. At each of the six call sites, replace `out=\"$(check \u2026 || true)\"` with `if out=\"$(check \u2026)\"; then echo \"\u274c self-test: <case> exited 0\"; return 1; fi`, then keep the existing greps. I applied exactly this to a scratch copy (review/C3-build-r2/mut2/fixed.sh). It passes under bash 5.3 and 3.2, and it kills return-0 at :226, compare-lints at :158, compare-bare at :154, zero-scan at :209, outside failed=1 at :203 and stale-row failed=1 at :215.\n2. The first fixture fails six ways at once, so dropping `failed=1` at :174 or :180 alone still exits 1 and survives. Give each path a fixture where it is the only failure:\n   - After the first fixture, repair every manifest except `lost` and assert a non-zero exit.\n   - Then create `lost/Cargo.toml` with one bad key and assert a non-zero exit.\n3. Rerun the mutation set with these eight exit-status mutations added, and update the count (\"Forty-one\") and the list in the e1c47b3f message and in the scripts/README.md self-test sentence to match."
    },
    {
     "where": "docs/adr/0008-two-binaries-one-daemon.md:44-58 (Finding 1, the paragraphs after \"`unsafe_code = \\\"deny\\\"` is set in `[workspace.lints.rust]`\")",
     "what": "ca7e3b81 makes this finding false at this commit:\n- The ADR says `unsafe_code` is \"re-declared in `crates/gglib-cli/Cargo.toml`\". That manifest is now only `[lints] workspace = true`.\n- It says \"`src-tauri/Cargo.toml` declares its own `[lints.rust]` carrying only `unreachable_pub`\u2026 So `gglib-app` \u2026 is not under the deny at all, and an `AttachConsole` call there would need no exemption \u2026 the lint does not reinforce it.\" src-tauri now inherits `unsafe_code = \"deny\"`, and its one unsafe module needed `#[allow(unsafe_code, reason = \u2026)]` on `mod layer_shell;` (src-tauri/src/tray/mod.rs:10-14).\nA reader weighing a merged binary is told the opposite of what is now true. ADR 0008 is Accepted, so CONTRIBUTING's \"An accepted ADR is frozen\" applies: a follow-up that landed goes in the ADR's log.",
     "fix": "1. Add docs/adr/log-0008.md. Use the standard log preamble (as in log-0009.md, without the \"moved verbatim\" sentence). Add one entry naming this change (commit ca7e3b81, or the C3 PR number once it exists). The entry should record that gglib-cli and src-tauri now inherit `[workspace.lints]`, that gglib-app is under `unsafe_code = \"deny\"`, and that an `AttachConsole` call there would need an `allow(unsafe_code)` exemption, as `mod layer_shell;` has.\n2. Add `- **Log:** [log-0008](log-0008.md)` to ADR 0008's header.\n3. Leave the ADR body unedited. If the orchestrator rules this out of scope for C3, file a follow-up issue with a number instead; it must not stay silent."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:22-23; scripts/README.md:139-140 (at the tip); e1c47b3f commit body, \"so the baseline's number of lints allowed without a reason can only fall\"",
     "what": "All three say the baseline's bare number \"can only fall\". Only `--update` is prevented from raising it. A hand edit of scripts/lint-allow-baseline.txt (e.g. `crates/gglib-proxy 77 10`) raises it, and the check accepts it. That is a monotonic claim, stronger than the no-growth guarantee this round asks every statement to be limited to. Minor: every other sentence in the header, README, CONTRIBUTING row and all four messages states only no-growth.",
     "fix": "- Header line 23: \"refuses to raise it, so that number can only fall\" \u2192 \"refuses to raise it, so `--update` never raises that number\".\n- README: \"so the number of lints allowed without a reason in the baseline can only fall\" \u2192 \"so `--update` never raises the baseline's number of lints allowed without a reason\".\n- e1c47b3f message: same replacement. Reword it at its own hash with --cleanup=whitespace and read it back."
    },
    {
     "where": "The plan's C3 PR subject and GitHub issue #1157. Orchestrator-owned: neither is in the commit range, so not the author's.",
     "what": "- The PR subject \"\u2026and the lints allowed without a reason can only fall\" is still false about the live count. A removal followed by an addition returns it to the baseline with rc 0 (round 1 took proxy's count from 8 to 9).\n- Issue #1157's body says the script \"counts the `allow(clippy::` occurrences per crate\". It counts every lint in any allow/expect, rustc lints included.\n- #1157 also says it \"closes when the baseline file holds no grandfathered allows\". The baseline holds counts, not attributes.",
     "fix": "- At PR creation, title the PR \"chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason may not grow\".\n- Edit #1157 to say the script counts every lint named in an allow/expect attribute.\n- Edit #1157 to say it closes when `git grep -c 'grandfathered at lint inheritance, #1157'` finds nothing."
    }
   ],
   "commands": "All commands ran in my own detached worktree review/C3-build-r2/gglib at 4197b907, which read clean before and after; I have since removed it. The build used CARGO_TARGET_DIR=targets/rv-gglib-proxy, CARGO_PROFILE_DEV_DEBUG=line-tables-only and CARGO_INCREMENTAL=0, with web_ui copied in.\n\n**History and authorship**\n- `git range-diff tmp/c3-base 591b698c 4197b907`: ca7e3b81 `=`, 98f37d3c `=`, 1f694177 `!` e1c47b3f, 591b698c `=` 4197b907. `git diff 591b698c 4197b907` touches only the script header (5+/5-).\n- Attribution grep: 0. The `^[a-z-]+-by:|^claude-session:` grep: 0.\n- Author and committer on all four commits: Matt O'Grady <sewing.wader8c@icloud.com>, %G? = N. Subjects are 54, 71, 78 and 59 characters, conventional and lowercase.\n\n**Script checks**\n- `bash -n` and `shellcheck` on the script: rc 0.\n- `--self-test` and the full check under bash 5.3.15 and /bin/bash 3.2.57: rc 0.\n\n**Repair 1: the self-test under a global `target/` ignore, and the caller's config for the real check** (VERIFIED)\n- The self-test passes (rc 0) with `target/` hidden through each of these:\n  - HOME/.config/git/ignore\n  - XDG_CONFIG_HOME/git/ignore\n  - GIT_CONFIG_GLOBAL with core.excludesFile\n  - GIT_CONFIG_GLOBAL with init.templateDir\n  - GIT_CONFIG_SYSTEM with excludesFile\n  - GIT_TEMPLATE_DIR with info/exclude\n  - GIT_CONFIG_PARAMETERS\n  - GIT_CONFIG_COUNT/KEY/VALUE\n  \n  All pass under bash 5; the combined hostile environment also passes under bash 3.2.\n- The pre-repair script (e92fa547) fails the self-test under the HOME ignore: \"an allow above the baseline passed\".\n- The real check still reads the caller's config. An untracked crates/gglib-db/src/zz_ignored_by_caller.rs with a bare allow fails the check in a plain environment (rc 1). It passes (rc 0) when hidden by HOME/.config/git/ignore, XDG_CONFIG_HOME, GIT_CONFIG_GLOBAL excludesFile or GIT_CONFIG_SYSTEM excludesFile, and under bash 3.2 too.\n- A standalone test confirms the `local` + `export` pattern: after the function returns, a caller's XDG_CONFIG_HOME is restored and GIT_CONFIG_GLOBAL/GIT_CONFIG_NOSYSTEM are unset again. The RETURN trap persists but does not fire when a later check() returns.\n\n**Repair 3: the four reasons and 98f37d3c's counts** (VERIFIED)\n- My own parser of the attributes 98f37d3c adds:\n  - 211 attributes naming 309 lints, none of them `#![...]`.\n  - 205 items: 142 fn, 7 struct, 1 static, 1 use, 54 mod.\n  - 201 reasons are \\\"grandfathered at lint inheritance, #1157\\\". The other 10 are 4 significant_drop_tightening, 1 cast_possible_truncation and 5 Tauri shapes.\n- Overlap checker, which I sanity-tested on two planted rows: 0 of 211 attributes repeat a lint an enclosing `mod` item already allows.\n- Stripping all 211 attributes and running clippy JSON on three targets gives exactly 489 distinct (crate, lint, file:line:col) findings, after excluding the duplicated_attributes and useless_attribute artefacts my substitution made. That matches \\\"491 \u2026 489 are allowed\\\".\n- Stripping only the four reasoned significant_drop_tightening allows raises the lint on all three targets at sampling_audit.rs:770, fixtures/common.rs:633, residency/launch.rs:174 and :285, residency/mod.rs:610, and supervisor.rs:312, :463 and :512. Each is a guard held over a check-and-mutate, so each allow is needed and its reason is true.\n- admission/state.rs holds a single cast (:737, `as_millis() as u64`). u64::MAX ms \u2248 584.9 million years.\n\n**Baseline**\n- An independent Python count of every member gives 496/182, matching lint-allow-baseline.txt row for row.\n- `--update` rewrites the file unchanged.\n\n**Repair 4: wording** (VERIFIED, with one nuance)\n- Every mention of growth in the diff and in all four messages states no-growth, except the \\\"can only fall\\\" nuance (defect 3).\n- The K7 comment line counts, 36 and 9, match e1c47b3f:Cargo.toml.\n\n**Rust gates**\n- `cargo clippy --workspace --all-targets --all-features -- -D warnings`, rc 0 on all three:\n  - aarch64-apple-darwin\n  - x86_64-unknown-linux-gnu (zig cc, fake pkg-config)\n  - x86_64-pc-windows-gnu\n  \n  Every workspace crate was rechecked.\n- `make enforce`: rc 0. check_rust_complexity reports \\\"no file over budget grew\\\", and the lint-inheritance check passes.\n- `./scripts/check_boundaries.sh`: rc 0.\n- `make bindings-check`: rc 0 (199 types).\n- `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps --document-private-items --exclude gglib-app`: rc 0.\n- `cargo test --no-fail-fast` on app-services, axum, cli, db, proxy, runtime, gglib-tauri and gglib-app: 2215 passed, 0 failed, 50 ignored, 69 result lines.\n- `cargo fmt --all -- --check`: rc 0.\n- generate_module_tables.sh does not exist at this commit.",
   "mutations": "Mutations were run on scratch copies of the tip script with `--self-test`, under bash 5.3. There were seven environments: the plain one and six hostile ones (xdg, global-excl, global-tpl, system, tpl-env; the author's own run has an all-channels one). The harness is review/C3-build-r2/mut2/run.py. The unmutated script passed in every environment before and after the run.\n\n**Repair 2: the named mutations**\nAll are KILLED in every environment:\n- (a) cfg_attr reads only its first allow: \\\"an allow above the baseline passed\\\".\n- (b) any line holding `//` dropped: \\\"an allow above the baseline passed\\\".\n- (c) bare counts attributes, not lints: \\\"--update raised the count of allows without a reason\\\".\n- (d) --update refuses to raise the first number: \\\"--update did not raise the count of allows\\\".\n\n**KILLED in every environment**\n- The per-line trailing `//` strip, comments counted, and cfg_attr ignored.\n- expect not counted, and cfg_attr's expect not counted.\n- Strings not blanked, bare = every lint, bare never counted, and reason counted as a lint.\n- Dropping --cached, --others, --exclude-standard or --deduplicate.\n- Deleted files listed, non-.rs files listed, a member matched by bare prefix, and outside files skipped.\n- Lints growth allowed (comparison removed) and bare growth allowed.\n- Update may raise bare, update writes nothing, and update writes even on failure.\n- Zero-scan guard removed.\n- [lints.x] accepted, an extra key accepted, and `workspace = <anything>` accepted.\n- The END tables branch or the END inherit branch removed.\n- Members comment not stripped, and members stops at the first line.\n\n**KILLED only in their own hostile channel**\nEach survives in the plain environment, as e1c47b3f says:\n- XDG_CONFIG_HOME dropped: killed in xdg.\n- GIT_CONFIG_GLOBAL dropped: killed in global-excl.\n- GIT_CONFIG_NOSYSTEM dropped: killed in system.\n- `--template=` dropped: killed in tpl-env.\n- The export line dropped: killed in xdg and system.\n\n**SURVIVED (defect 1)**\n- check's `return \\\"$failed\\\"` changed to `return 0`.\n- compare's lints `return 1` changed to `return 0`.\n- The zero-scan `return 1` changed to `return 0`.\n- `failed=1` dropped after the missing-manifest echo, the bad-manifest echo, the outside-members failure or the stale-row echo.\n\nOnly compare's bare `return 1` \u2192 `return 0` is KILLED (\\\"an allow outside every member passed\u2026\\\"). With the if-out-then-fail fix applied to a scratch copy, the return-0, compare-lints, compare-bare, zero-scan, outside and stale-row mutations are KILLED. The two manifest `failed=1` drops still survive until each gets a fixture of its own.\n\n**On the real tree**, each caught by `check_lint_inheritance.sh` with rc 1:\n- `rust = { unsafe_code = \\\"allow\\\" }` added to gglib-proxy's `[lints]`: \\\"[lints] holds `rust = \u2026`\\\".\n- The reason stripped from the admission cast allow: \\\"crates/gglib-runtime: 18 lints allowed without a reason, baseline 17\\\".\n\n**Allow-removal mutations** (clippy, all three targets)\n- The four reasoned significant_drop_tightening allows removed: 8 findings raised.\n- All 211 added attributes stripped: 489 findings raised.\n\nEvery restore was a `git checkout` followed by a touch, and the tree read clean afterwards."
  },
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "base": "tmp/c3-base (c0c951c8)",
   "head_sha": "d5f6a5ec65b738a23cddf07560dea8362b6149fc",
   "commits": [
    "1833950f chore(lints): every crate inherits the workspace lints (was ca7e3b81; adds docs/adr/log-0008.md and the ADR 0008 header Log line; the body gains one paragraph about them)",
    "ebbf75f3 chore(lints): the remaining findings are grandfathered where they stand (was 98f37d3c; range-diff =)",
    "dbfe3d44 chore(lints): enforcement checks lint inheritance and each crate's allow count (was e1c47b3f; self-test exit-status fix, single-failure manifest fixtures, header and README wording, message reworded)",
    "d5f6a5ec chore(lints): the visibility lints' comments state the rule (was 4197b907; range-diff =)"
   ],
   "summary": "I fixed all three defects that belong to the commit range. The fourth concerns the PR title and issue #1157, which the orchestrator owns, so I left it alone. Nothing is pushed.\n\nDefect 1 (the self-test ignored check's exit status), in scripts/check_lint_inheritance.sh:\n- All eight fixtures that must fail now run `if out=\"$(check \u2026)\"; then echo \"\u274c self-test: <case> exited 0\"; return 1; fi` before their message greps. The eight are: the first fixture, the two new manifest fixtures, growth, outside, --update over a bare allow, the stale row and the zero scan.\n- The two new fixtures come right after the first one. The other five manifests are repaired first. Then `lost`, with no manifest, must exit non-zero with exactly one \u274c line. Then `lost/Cargo.toml` gets `rust = { unsafe_code = \"allow\" }` beside `workspace = true`, and the same assertion applies. Then `lost` is repaired for the clean-pass fixture.\n- The zero-scan fixture now rewrites the baseline without the `gone` row, so the empty listing is its only failure.\n- I updated the header, the \u2713 line, the self-test sentence in scripts/README.md and the e1c47b3f body to match. The body's count went from \"Forty-one\" to \"Fifty-four\", which is the measured number.\n\nDefect 2 (ADR 0008 finding 1 is false after the inheritance change):\n- New file docs/adr/log-0008.md, with the standard preamble and one entry dated 2026-09-26. It records that `crates/gglib-cli/Cargo.toml` no longer re-declares `unsafe_code = \"deny\"`, and that `src-tauri/Cargo.toml` no longer has its own `[lints.rust]`. So gglib-app is under the deny, and an `AttachConsole` call there would need an `allow(unsafe_code)` exemption, as `mod layer_shell;` in src-tauri/src/tray/mod.rs has. It also says the finding's \"is not under the deny at all\" no longer holds, and that the `windows_subsystem` attribute the finding rests on is unchanged.\n- ADR 0008's header gains `- **Log:** [log-0008](log-0008.md)`. The ADR body is not edited. Both changes are in 1833950f, whose body now says so.\n\nDefect 3 (\"can only fall\"): I deleted the clause in the script header, scripts/README.md and the e1c47b3f body. `git grep 'can only fall'` at HEAD finds nothing, and neither does a grep of the range's messages.\n\nChecks:\n- The tree after the rebase matches the pre-rebase fixup tip exactly.\n- Both reworded messages match their source files byte for byte when read back at their own hashes.\n- The attribution-trailer grep counts 0. Author and committer on all four commits are Matt O'Grady <sewing.wader8c@icloud.com>.\n- `make enforce` passes at the tip, including the lint-inheritance check and check_adrs.py.\n- Each commit was dev-checked from a clean tree (dirty=0).\n- Mutations: 54 of 54 are KILLED under bash 5.3 and under /bin/bash 3.2. Against the old script, the seven exit-status mutations the reviewer named SURVIVE in the same harness. That shows the harness detects them and the fix is what kills them.\n\nFiles: /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/scripts/check_lint_inheritance.sh, /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/scripts/README.md, /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/docs/adr/log-0008.md, /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/docs/adr/0008-two-binaries-one-daemon.md. Harness and logs: /private/tmp/claude-501/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6/scratchpad/c3fix3/.",
   "commands_run": "All commands ran in /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib. The effective git identity is Matt O'Grady <sewing.wader8c@icloud.com>; the local config overrides the global `mmogr`.\n\nBefore committing:\n- `bash -n` and `shellcheck scripts/check_lint_inheritance.sh`: rc 0.\n- `bash \u2026 --self-test` and `/bin/bash \u2026 --self-test`: rc 0.\n- The full check under bash 5.3.15 and 3.2.57: rc 0 with \"\u2705 every member inherits\u2026\".\n- Real-tree probe: an untracked crates/gglib-db/src/zz_new.rs holding `#[allow(clippy::too_many_lines)]` failed the check with rc 1 (\"\u274c crates/gglib-db: 1 lints allowed without a reason, baseline 0\"). I removed the file, reran the check (rc 0), and `git status` was clean.\n- `python3 scripts/check_adrs.py --check`: rc 0 once log-0008.md was intent-added. Before that it gave the expected \"not a tracked file\" error.\n\nMutations: `python3 c3fix3/run.py bash` and `python3 c3fix3/run.py /bin/bash` against the working-tree script, plus `python3 c3fix3/run.py bash c3fix3/old.sh` against the 4197b907 script (see mutations).\n\nHistory:\n- `git commit --no-gpg-sign --fixup=ca7e3b81` (docs/adr) and `--fixup=e1c47b3f` (scripts).\n- `GIT_SEQUENCE_EDITOR=c3fix3/seqed.sh git rebase -i --autosquash tmp/c3-base`. That is autosquash plus two `exec git commit --amend --no-gpg-sign --cleanup=whitespace -F <msg>` lines after the fixups. I read the todo back from c3fix3/todo.after.\n\nVerification after the rebase:\n- `git diff b1fb39d6 HEAD` (the pre-rebase fixup tip) is empty.\n- `cmp` of each reworded message file against `git log -1 --format=%B` at 1833950f and at dbfe3d44: identical.\n- `git range-diff tmp/c3-base 4197b907 HEAD`: `!` on commits 1 and 3, `=` on 2 and 4.\n- The attribution grep over `git log --format=%B tmp/c3-base..HEAD` prints 0. `grep -c 'can only fall'` prints 0, and `git grep 'can only fall' HEAD` finds nothing.\n- Author, committer and %G? on all four commits: Matt O'Grady <sewing.wader8c@icloud.com>, N.\n\nGate at the tip (log c3fix3/tip-enforce.log, first line `d5f6a5ec\u2026 dirty=0`):\n- `make enforce`: rc 0, including check_rust_complexity, check_lint_inheritance and check_adrs.\n- `check_adrs.py --check`: rc 0.\n\nPer-commit dev checks (logs c3fix3/dev-<hash>.log, each starting with its hash and dirty=0; I detached the same worktree at each commit and returned to the branch after):\n- 1833950f: check_adrs rc 0.\n- dbfe3d44 and d5f6a5ec: check_adrs rc 0, shellcheck rc 0, and the self-test and full check each rc 0 under bash 5.3 and 3.2.\n\nNot run:\n- No cargo commands. No Rust source, manifest or ts-rs type changed in this round; the only changes are a shell script, Markdown files and commit messages.",
   "mutations": "Harness: c3fix3/run.py. Each mutation is applied to a scratch copy (c3fix3/scripts/m.sh) and run with `--self-test` in seven environments:\n- plain: the real HOME, whose global ignore holds only `**/.claude/settings.local.json`\n- xdg: HOME/.config/git/ignore holds `target/`\n- global-excl: GIT_CONFIG_GLOBAL with an excludesFile\n- global-tpl: GIT_CONFIG_GLOBAL with init.templateDir\n- system: GIT_CONFIG_SYSTEM with an excludesFile\n- tpl-env: GIT_TEMPLATE_DIR with info/exclude\n- all: every channel at once\n\nThe worktree is never mutated. The unmutated script passes in all seven before and after each run.\n\nFixed script, 54 mutations, bash 5.3 (log c3fix3/new-bash5.log): 54 KILLED, 0 SURVIVED. /bin/bash 3.2 (c3fix3/new-bash3.log): 54 KILLED, 0 SURVIVED.\n\nExit-status mutations: the message stays and only the failure goes. Each is shown with the self-test line that caught it:\n- check's `return \"$failed\"` \u2192 `return 0`: \"the first fixture exited 0\"\n- compare's bare `return 1` \u2192 `return 0`: \"an allow outside every member exited 0\"\n- compare's lints `return 1` \u2192 `return 0`: \"an allow above the baseline exited 0\"\n- zero-scan `return 1` \u2192 `return 0`: \"a tree with no .rs file listed exited 0\"\n- missing-manifest `failed=1` dropped: \"a member with no manifest exited 0\"\n- bad-manifest `failed=1` dropped: \"a manifest with a key beside `workspace = true` exited 0\"\n- member `compare \u2026 || failed=1` dropped: \"an allow above the baseline exited 0\"\n- outside-row `failed=1` dropped: \"an allow outside every member exited 0\"\n- stale-row `failed=1` dropped: \"a baseline row for no member exited 0\"\n\nControl, the same harness on the 4197b907 script (c3fix3/old-bash5.log): 47 KILLED and 7 SURVIVED. The survivors are the reviewer's seven: check return 0, compare-lints return 0, zero-scan return 0, and failed=1 dropped at missing manifest, bad manifest, outside row and stale row. Against that script, the bare return 0 is killed by the outside grep, and the member `|| failed=1` drop by the --update baseline grep.\n\nThe other 45 mutations are KILLED in every environment:\n- counting: cfg_attr ignored, expect not counted, cfg_attr expect not counted, cfg_attr reads its first allow only, strings not blanked, comments counted, any line holding // dropped, trailing // stripped per line, reason counted as a lint, bare = every lint, bare never counted, bare counts attributes not lints\n- manifests: [lints.x] accepted, extra key accepted, END block emptied, END tables branch removed, END inherit branch removed, workspace = <anything> accepted, missing manifest accepted (`if false`)\n- members: parser reads one per line, stops at the first line, comment not stripped\n- file listing: each of --cached, --others, --exclude-standard, --deduplicate dropped; deleted files listed; non-.rs listed; count only src/; member bare prefix; outside skipped; outside bare prefix\n- comparison and update: lints growth allowed, bare growth allowed, update may raise bare, update refuses to raise lints, update writes nothing, update writes even on failure, stale row accepted (`if false`), zero-scan guard removed\n\nThe exceptions are the five isolation mutations. Each is KILLED only in its own hostile channel and in `all`, and survives in plain, as the header says it should:\n- XDG_CONFIG_HOME dropped: killed in xdg and all\n- GIT_CONFIG_GLOBAL dropped: global-excl and all\n- GIT_CONFIG_NOSYSTEM dropped: system and all\n- `--template=` dropped: tpl-env and all\n- export line dropped: xdg, system and all\n\nThe real-tree probe is under commands_run.",
   "deviations": "Defect 1 (the self-test never checked check's exit status): fixed in dbfe3d44. This is the reviewer's if-out-then-fail change at every failing call site, plus two fixtures where one manifest path is the only failure: `lost` missing, then `lost` with one bad key. Both assert a non-zero exit and exactly one \u274c line. Beyond the reviewer's fix, the zero-scan fixture now resets the baseline so the stale `gone` row is not a second failure. The e1c47b3f body's count is now \"Fifty-four\", the measured number, and it names the nine exit-status mutations and the export-line isolation mutation. The README self-test sentence and the script header list the new fixtures and the exit rule.\nDefect 2 (ADR 0008 finding 1 false after ca7e3b81): fixed in 1833950f, with docs/adr/log-0008.md and the header Log line; the ADR body is untouched. The entry names no hash because folding it in rewrites ca7e3b81, so a commit cannot name its own hash, and the PR has no number yet. It names the change by what it did and cites #1157, the handle its grandfathered allows already use. If the orchestrator wants the PR number in the entry, it has to be added before merge, because a landed log is append-only.\nDefect 3 (\"can only fall\"): fixed in dbfe3d44, in the header, the README and the e1c47b3f body. I deleted the clause rather than using the suggested replacement: in the header it would have read \"`--update` refuses to raise it, so `--update` never raises that number\", which repeats itself. What is left says `--update` refuses to raise the second number, which is exactly the no-growth claim.\nDefect 4 (PR subject and #1157 body): disputed as mine, not as wrong. Neither is in the commit range, and the brief forbids editing the plan or touching GitHub, so I changed nothing. I agree with the reviewer's text. Title the PR \"chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason may not grow\". The plan's C3 subject still ends \"can only fall\". Edit #1157 to say the script counts every lint named in an allow/expect attribute, rustc lints included. Edit it to say the issue closes when `git grep -c 'grandfathered at lint inheritance, #1157'` finds nothing.",
   "review_focus": "1. docs/adr/log-0008.md is new prose, so review it as hard as a first draft. In particular:\n   - \"The change that made every workspace member inherit `[workspace.lints]`, whose grandfathered allows #1157 tracks\". This names no commit, but the preamble's \"a line number refers to the commit the entry names\" still stands. The entry cites no line numbers.\n   - \"The `windows_subsystem = \"windows\"` attribute \u2026 on which the finding rests, is unchanged\". 98f37d3c edits src-tauri/src/main.rs (setup_app), but not line 2.\n2. The fixture order in selftest(). `lost` stays missing through the first fixture and the missing-only fixture. It then gets a bad manifest, and is repaired just before the clean-pass fixture, so the clean pass still covers all seven members. The zero-scan fixture now writes `$expected` into the baseline after removing the index. Check that the zero-scan grep ('found 7 members and 0 .rs files') still holds.\n3. The claim \"each as the only failure\" (header, README and e1c47b3f body) is backed only for the two manifest fixtures, by `grep -c '^\u274c'` = 1. The other failing fixtures are asserted on exit status and message, not on being alone, and no prose claims they are alone.\n4. The e1c47b3f body's mutation paragraph: \"Fifty-four \u2026 under bash 5.3 and 3.2\", the nine exit-status mutations, and \"The five isolation mutations \u2026 each was run with one that does\". Compare it with c3fix3/new-bash5.log and new-bash3.log.\n5. The single sentence added to the 1833950f body: \"Its new log \u2026 records that both binaries now are\". The CLI was under the deny before this change, through its own table."
  },
  {
   "verdict": "FAIL",
   "defects": [
    {
     "where": "scripts/check_lint_inheritance.sh:148-162 (compare), in dbfe3d44. The false guarantee is also stated at header lines 21-23, the scripts/README.md sentence \"It raises the first number but refuses to raise the second\", and the dbfe3d44 body sentence \"it will raise the first number but not the second\".",
     "what": "The gate passes when a baseline row is not exactly one pair of integers, or when perl fails to produce a count. In those cases `[ \"$3\" -gt \"$base_bare\" ]` errors with \"integer expected\" (status 2). The if condition is then false, so compare returns 0. `check` runs inside `if`, where set -e does not apply, so nothing else stops it. I reproduced all three cases at d5f6a5ec with an untracked crates/gglib-db/src/zz_probe.rs holding `#[allow(clippy::too_many_lines)]` (control: \"\u274c crates/gglib-db: 1 lints allowed without a reason, baseline 0\", rc 1). (i) With the `crates/gglib-db 9 0` row duplicated, which is what keeping both sides of a conflict produces, the output is \"line 154: [: 0\\n0: integer expected\" and then \"\u2705 every member inherits the workspace lints\u2026\", rc 0. (ii) `--update` in the same state prints \"\u2705 baseline updated\" and rewrites the row to `crates/gglib-db 10 1`. That raises the second number, which the header, README and commit body say --update refuses to do. A non-numeric second field (`crates/gglib-db 9 none`) under --update does the same. (iii) With the probe file set to chmod 000, perl dies (\"Permission denied at -e line 12\"), both comparisons error, and the check prints \u2705 with rc 0. The member's other 9 allows go uncounted as well. Why it matters: every PR that adds or pays down an allow edits this one file, so a merge conflict there is ordinary. Once a row is duplicated, that member's check is off without any failure, and `--update` then raises its no-reason count. That breaks the no-growth rule and the header's exit-code contract (line 40: 1 when a rule failed).",
     "fix": "In compare, after the `${\u2026:-0}` defaults, fail closed unless each of \"$2\" \"$3\" \"$base_lints\" \"$base_bare\" is one run of digits: `local v; for v in \"$2\" \"$3\" \"$base_lints\" \"$base_bare\"; do case \"$v\" in '' | *[!0-9]*) echo \"\u274c $1: a count or its baseline row is not one number each\"; return 1 ;; esac; done`. A duplicated row yields \"9\\n9\" and a failed count yields \"\", so this one check covers both. I prototyped exactly this. The self-test still passes under bash 5.3 and 3.2. The duplicated row then fails with \"\u274c crates/gglib-db: a count or its baseline row is not one number each\" / \"\u274c baseline not updated\", rc 1, and the baseline is not rewritten. Add self-test fixtures, each asserting a non-zero exit (if out=\u2026; then fail) and the message: a baseline with the `good` row duplicated, a baseline whose `good` second field is not a number, and `--update` over the duplicated row, which must exit non-zero and leave the baseline file byte-identical. Mutation-check it by deleting the validation loop: the new fixtures must go red. Then update the header's self-test paragraph, the README sentence, the dbfe3d44 body's fixture list and its mutation count (\"Fifty-four\") to match what is measured."
    },
    {
     "where": "scripts/check_lint_inheritance.sh:386-395 (the outside-row fixture), line 203 (the outside compare call), header line 34, the scripts/README.md phrase \"baselines that each count must fail against\", and the dbfe3d44 body phrase \"It has baselines each count must fail against\"",
     "what": "No fixture makes the (outside-members) row's first number fail. The only outside fixture sets that row's second number to 0 (`$3 = 0`), so only the bare count is exercised against the outside row. Mutation: change line 203 `compare \"$OUTSIDE\" \"$lints\" \"$bare\" \"$baseline\" \"$update\"` to `\u2026 \"$baseline\" x`, so the outside row's lint count is never compared. It SURVIVES under bash 5.3 and 3.2: `--self-test` prints \"\u2713 self-test: \u2026\" with rc 0. So the prose claim that each count has a baseline it must fail against is false for one of the fixture's four counts, and the header's promise that the outside files are held \"the same way\" is untested for the first number.",
     "fix": "Add a fixture with the baseline at $expected except `(outside-members) 0 1` (awk -v o=\"$OUTSIDE\" '$1 == o { $2 = 0 } 1'). Assert that check exits non-zero, prints \"(outside-members): 1 lints allowed, baseline 0\", and prints \"counted in goodies/shared.rs\". Rerun the line-203 mutation and show it KILLED. Recount the mutations in the dbfe3d44 body if you add this one to the harness."
    },
    {
     "where": "Outside the commit range and not the author's to fix, because the orchestrator owns it: the PR title the plan gives C3, and issue #1157's body",
     "what": "Round 2's defect 4 is still open. The plan's C3 subject still reads \"\u2026and the lints allowed without a reason can only fall (narrowed in review 2026-09-26: \u2026)\". Squash-merge makes the PR title the subject on main, and the check is a no-growth count: a bare count can drop and then climb back to its baseline without failing. #1157's body (read via gh at 2026-09-26) still says the script counts \"the `allow(clippy::` occurrences per crate\" and that the issue closes \"when the baseline file holds no grandfathered allows\". The script counts every lint named in allow/expect, rustc lints included, and the baseline file holds numbers, not attributes.",
     "fix": "Orchestrator: open the PR with the title \"chore(lints): every crate is held to the workspace lints, and the lints allowed without a reason may not grow\". Edit #1157 to say the script counts every lint named in an allow/expect attribute (cfg_attr forms, rustc lints included) against a no-growth baseline, and that it closes when `git grep -c 'grandfathered at lint inheritance, #1157'` finds nothing. Neither change touches the author's commits."
    }
   ],
   "commands": "I worked in my own worktree at .../scratchpad/review/C3-build-r3/gglib, detached at d5f6a5ec, with web_ui copied in. It was clean (dirty=0) for every gate, and I removed it at the end. I left the target in place. Everything built with CARGO_TARGET_DIR=.../targets/rv-gglib-proxy CARGO_PROFILE_DEV_DEBUG=line-tables-only CARGO_INCREMENTAL=0. Logs are in .../review/C3-build-r3/w/logs, and each gate log starts with d5f6a5ec and dirty=0.\n\nGates:\n- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: rc 0 on aarch64-apple-darwin, and on x86_64-unknown-linux-gnu and x86_64-pc-windows-gnu (cross-checked with the zig wrappers). All 18 gglib crates were rechecked on each target; the only output is the 36 known ts-rs serde-attribute notes.\n- `make enforce`: rc 0. The self-test \u2713, \\\"\u2705 every member inherits\u2026\\\", the Rust and TS complexity ratchets and check_adrs all pass.\n- `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps --document-private-items --exclude gglib-app`: rc 0, 17 crates documented.\n- `make bindings-check`: rc 0, 199 types, tree clean afterwards. `check_boundaries.sh`: rc 0. `cargo fmt --all -- --check`: rc 0.\n- `shellcheck`, `bash -n` and `/bin/bash -n` on the script: rc 0. It is tracked as 100755.\n- `python3 scripts/check_adrs.py --check`: rc 0.\n- `--update` at the tip leaves scripts/lint-allow-baseline.txt byte-identical, so the baseline equals the tree's counts (496 lints across 18 members, 182 bare, outside 0 0).\n- Attribution grep over tmp/c3-base..HEAD: 0. Author and committer on all 4 commits: Matt O'Grady <sewing.wader8c@icloud.com>. `git ls-remote origin 'refs/heads/chore(lints)/*'`: empty, so nothing is pushed.\n- No fenced code block is removed in any .md in the range (0 removed, 2 added).\n\nRound-1 repairs, all verified:\n- (1) The self-test passes (rc 0) under bash 5.3 and 3.2 when `target/` is ignored through each hostile channel: XDG_CONFIG_HOME/git/ignore, HOME/.config/git/ignore, HOME/.gitconfig core.excludesFile, GIT_CONFIG_GLOBAL excludesFile, GIT_CONFIG_SYSTEM excludesFile, a global init.templateDir, GIT_TEMPLATE_DIR, and GIT_CONFIG_COUNT. A control repo confirms each channel really hides src/target/x.rs.\n- The real check still reads the caller's config. With an untracked crates/gglib-db/src/zz_probe.rs holding a bare allow and `zz_probe.rs` ignored through xdg, home1, home2, global or system, the check passes (rc 0). With no ignore it fails (rc 1). The same holds under both bashes.\n- (2) The round-1 mutations are all KILLED under both bashes: comment filter drops any line with //, bare counts attributes, --update refuses to raise lints, cfg_attr while\u2192if, and cfg_attr allow only.\n- (3) The four significant_drop_tightening allows sit on gglib-proxy `mod sampling_audit`, tests/fixtures `mod common` (ResidentSimSlot's StdMutex, common.rs:570-640), runtime `mod residency` and runtime proxy `mod supervisor`, each with the lock-code reason. My parser (w/attrs.py) over 1833950f..ebbf75f3 finds 211 attributes naming 309 lints on 205 items: 142 fn, 7 struct, 1 static, 1 use and 54 mod, with 6 items carrying two attributes. None is an inner attribute. 201 carry the #1157 reason and 10 carry another (4+2+1+1+1+1). All of this matches the ebbf75f3 body.\n- (4) `git grep 'can only fall' HEAD` and a grep of every message both find nothing.\n\nRound-2 repairs:\n- The exit-status fixtures are fixed. All nine exit mutations are KILLED; see mutations.\n- log-0008.md is checked against the tree. `[lints] workspace = true` is in both manifests. `[workspace.lints.rust] unsafe_code = \\\"deny\\\"` is at Cargo.toml:147. src-tauri's package name is gglib-app. `mod layer_shell;` is at src-tauri/src/tray/mod.rs:14 with `allow(unsafe_code, reason=\u2026)`. main.rs line 2 (windows_subsystem) is unchanged in the range. At 0b338d9e (the ADR's own commit) both manifests had their own [lints.*] tables. The ADR header's Log line matches CONTRIBUTING's frozen-ADR rule. The entry cites no line number. #1157 exists and is OPEN.\n- The new 1833950f paragraph is true.\n- dbfe3d44 claims I checked: 977 .rs files under the main checkout's .claude/, and build_common.rs `include!`d by the members' build scripts. The author's logs show 54/54 KILLED under both bashes.\n\nBehaviour probes that found defect 1: a duplicated baseline row, a non-numeric field (with and without --update), and a chmod-000 probe file. A CRLF baseline, by contrast, fails correctly (bash's `[` tolerates a trailing \\\\r), so it is not a trigger. I also prototyped the suggested fix and checked it. awk on CI is mawk, which I could not run locally (no mawk, docker daemon down); CI at PR creation covers it.",
   "mutations": "Harness: w/mut.py. It writes each mutated copy to w/mscript/m.sh (the script under review is never touched) and runs `--self-test` with /opt/homebrew/bin/bash 5.3.15 and /bin/bash 3.2.57. A mutation counts as KILLED only when rc\u22600 and the first self-test line is a \\\"\u274c self-test:\\\" line, so a crash is not scored as a kill. The unmutated script passes in all 7 environments before and after the run. Results are identical under both bashes (logs/mut-bash5.log, logs/mut-bash3.log).\n\nRound 1:\n- a: cfg_attr reads its first allow only \u2192 KILLED, \\\"an allow above the baseline exited 0\\\"\n- b: any line holding // dropped \u2192 KILLED, same line\n- c: bare counts attributes, not lints \u2192 KILLED, \\\"--update over an allow without a reason exited 0\\\"\n- r1-c: --update refuses to raise lints \u2192 KILLED, \\\"--update did not raise the count of allows\\\"\n- r1-e: cfg_attr allow only \u2192 KILLED, \\\"an allow above the baseline exited 0\\\"\n- r1-q: trailing // stripped per line \u2192 KILLED, same line\n- update writes even on failure \u2192 KILLED, \\\"--update raised the count of allows without a reason\\\"\n\nRound 2, exit status:\n- x1: check returns 0 \u2192 KILLED, \\\"the first fixture exited 0\\\"\n- x2: compare's bare `return 1`\u21920 \u2192 KILLED, \\\"an allow outside every member exited 0\\\"\n- x3: compare's lints `return 1`\u21920 \u2192 KILLED, \\\"an allow above the baseline exited 0\\\"\n- x4: zero-scan `return 1`\u21920 \u2192 KILLED, \\\"a tree with no .rs file listed exited 0\\\"\n- x5: missing-manifest failed=1 dropped \u2192 KILLED, \\\"a member with no manifest exited 0\\\"\n- x6: bad-manifest failed=1 dropped \u2192 KILLED, \\\"a manifest with a key beside `workspace = true` exited 0\\\"\n- x7: member `|| failed=1` dropped \u2192 KILLED, \\\"an allow above the baseline exited 0\\\"\n- x8: outside failed=1 dropped \u2192 KILLED, \\\"an allow outside every member exited 0\\\"\n- x9: stale-row failed=1 dropped \u2192 KILLED, \\\"a baseline row for no member exited 0\\\"\n\nIsolation, each run in its hostile channel (all survive in plain, as documented):\n- i1: XDG_CONFIG_HOME dropped \u2192 KILLED in xdg and in HOME/.config\n- i2: GIT_CONFIG_GLOBAL dropped \u2192 KILLED in global-excludes; survives in global-init.templateDir, because --template= also covers that channel\n- i3: GIT_CONFIG_NOSYSTEM dropped \u2192 KILLED in system\n- i4: --template= dropped \u2192 KILLED in GIT_TEMPLATE_DIR\n- i5: export line dropped \u2192 KILLED in the HOME/.config channel; survives when the caller exports XDG_CONFIG_HOME, because the local inherits the export attribute. This is consistent with the body's \\\"only when \u2026 and each was run with one that does\\\".\n\nOther behaviours:\n- [lints.x] accepted \u2192 KILLED, \\\"did not fail on exactly the six bad manifests\\\"\n- members parser reads one per line \u2192 KILLED, same line\n- drop --others \u2192 KILLED, \\\"an allow above the baseline exited 0\\\"\n- drop --exclude-standard \u2192 KILLED, first-fixture line\n- lints growth allowed \u2192 KILLED, \\\"an allow above the baseline exited 0\\\"\n- update may raise bare \u2192 KILLED, \\\"--update over an allow without a reason exited 0\\\"\n- stale row accepted \u2192 KILLED, \\\"a baseline row for no member exited 0\\\"\n- comments counted \u2192 KILLED, first-fixture line\n- strings not blanked \u2192 KILLED, first-fixture line\n- outside row's lint count never compared (line 203 update arg \u2192 x) \u2192 SURVIVED under both bashes. This is defect 2.\n- `tables != 1` \u2192 `tables == 0` \u2192 SURVIVED. Not a defect: two [lints] tables is invalid TOML that Cargo already rejects.\n\nBehaviour probes, not mutations, on the real tree:\n- A duplicated gglib-db row gives \u2705 rc 0, and --update then rewrites the row to 10 1.\n- A non-numeric second field under --update rewrites the row to 10 1.\n- A chmod-000 file gives \u2705 rc 0.\nAll three are defect 1. With the prototype fix, the duplicated row fails with rc 1 and \\\"baseline not updated\\\", and the self-test still passes."
  }
 ],
 "wf_c312adf7-494": [
  {
   "status": "done",
   "branch": "chore(lints)/every-crate-is-held-to-the-workspace-lints",
   "head_sha": "6e8e79664e5253232b4648adccca62a6cd727507",
   "base": "tmp/c3-base (c0c951c87e28eae6f8f898e657d99543c3829172)",
   "commits": [
    "1833950f9a7c0be5f26eb82ab65a10c6c9cb8a8a chore(lints): every crate inherits the workspace lints (unchanged)",
    "ebbf75f33756553fc3b16b4460435557ecb75983 chore(lints): the remaining findings are grandfathered where they stand (unchanged)",
    "7931a83b975a26891dfcc96d42534bb6bbafc293 chore(lints): enforcement checks lint inheritance and each crate's allow count (was dbfe3d44; fixup folded in, body amended)",
    "6e8e79664e5253232b4648adccca62a6cd727507 chore(lints): the visibility lints' comments state the rule (was d5f6a5ec; re-parented only, its diff is byte-identical)"
   ],
   "summary": "Both build-lens defects are fixed and folded into dbfe3d44, now 7931a83b. Only scripts/check_lint_inheritance.sh and scripts/README.md changed. Nothing is pushed.\n\n1. The fail-open is closed. After the defaults, compare() now checks that \"$2\", \"$3\", \"$base_lints\" and \"$base_bare\" are each one run of digits. If one is not, it prints \"\u274c <row>: a count or its baseline row is not one number each\" and returns 1. Because any failure stops `--update` from writing, `--update` now writes nothing in that case.\n   - Before the fix, measured on the real tree: a doubled `crates/gglib-db` row printed the pass line with rc 0, and `--update` removed the doubled row. A chmod 000 on crates/gglib-db/src/lib.rs also printed the pass line with rc 0, with perl's \"Permission denied\" and \"[: : integer expected\" on stderr.\n   - After the fix: the doubled row gives the new \u274c line and rc 1, and `--update` gives rc 1 and leaves the baseline byte-identical (cmp), under bash 5.3.15 and 3.2.57. The unreadable file gives the new \u274c line and rc 1 under both.\n   - New self-test fixtures, each asserting a non-zero exit and the message:\n     - the `good` row listed twice;\n     - `--update` over that doubled row, which must also leave the baseline byte-identical (cmp -s);\n     - `good` with `x` as its first number, then as its second;\n     - a counter that exits 1 (`COUNTER='exit 1' check ...` inside the `$(...)` subshell).\n\n2. The (outside-members) row's first number is now tested. A new fixture uses the expected baseline with `(outside-members) 0 1` and asserts a non-zero exit, \"(outside-members): 1 lints allowed, baseline 0\" and \"counted in goodies/shared.rs\". The :203 mutation (the outside compare always passed `update`) SURVIVED on the old script and is KILLED on the new one.\n\nThe header's self-test paragraph, a rule-2 sentence in the header, the README paragraph, the \u2713 line and 7931a83b's body now describe only what was measured. The body's mutation paragraph now says 62 of 64 mutations are killed and names the 2 that survive.\n\nFiles: /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/scripts/check_lint_inheritance.sh and /Users/mattogrady/.local/src/worktrees/gglib/lane-proxy/gglib/scripts/README.md. Mutation runner and logs: /private/tmp/claude-501/-Users-mattogrady--local-src-gglib/d2f123ee-56ab-4c4c-a881-dbf2eef763a6/scratchpad/c3fix4/ (run.py, new-bash5.log, new-bash3.log, old-bash5.log, dev-7931a83b.log, dev-6e8e7966.log, tip-enforce.log, msg-dbf.txt).",
   "commands_run": "- Reproduced the defects on the old script against the real tree, with bash 5: a doubled baseline row gave \u2705 and rc 0, and `--update` rewrote the baseline (cmp showed the doubled row gone); chmod 000 on crates/gglib-db/src/lib.rs gave \u2705 and rc 0. File mode and baseline restored afterwards (`git status` clean apart from my edits).\n- After the fix, same cases with bash and /bin/bash: \u274c and rc 1 for both; `--update` over the doubled row gave rc 1 and a byte-identical baseline.\n- `shellcheck scripts/check_lint_inheritance.sh` (0.11.0): rc 0.\n- `bash scripts/check_lint_inheritance.sh --self-test` and `/bin/bash scripts/check_lint_inheritance.sh --self-test`: rc 0 under both.\n- `python3 run.py {bash,/bin/bash} new.sh` in scratchpad/c3fix4: each self-test run in 7 git-config environments. Unmutated: all 0. 64 mutations: 62 KILLED, 2 SURVIVED, same result under both shells.\n- `python3 run.py bash old.sh`: 54 KILLED, 9 BADANCHOR (the new-loop mutations have nothing to mutate there), and the :203 outside-lints mutation SURVIVED, confirming defect 2.\n- `git commit --no-gpg-sign --fixup=dbfe3d44`, then `GIT_SEQUENCE_EDITOR=seqed.sh git rebase -i --autosquash tmp/c3-base`, with an exec that runs `git commit --amend --cleanup=whitespace -F msg-dbf.txt`. Checks after the rebase:\n  - the tip tree equals the pre-rebase fixup tip (47690ff6);\n  - `git diff dbfe3d44 7931a83b` equals the fixup diff;\n  - 6e8e7966's diff equals d5f6a5ec's;\n  - the message reads back identical to the file;\n  - the trailer grep prints 0;\n  - author on all four commits is Matt O'Grady <sewing.wader8c@icloud.com>.\n- Dev-check of 7931a83b in a temporary detached worktree (log starts `7931a83b\u2026 dirty=0`): shellcheck 0; self-test and check 0 under bash and /bin/bash. check_adrs.py returned 2 in that log because the dev-check script left out `--check`; it passed at the tip.\n- Dev-check at the tip 6e8e7966 (dirty=0): shellcheck, self-test and check under both shells, and `check_adrs.py --check`, all rc 0.\n- `make enforce` at the tip: rc 0.\n- No Rust changed, so no cargo fmt, clippy or cargo tests were run.",
   "mutations": "Runner: scratchpad/c3fix4/run.py. The 54 earlier mutations plus 10 new ones, each run as `--self-test` in 7 git-config environments, under bash 5.3.15 and /bin/bash 3.2.57. Results are the same under both shells. Each mutant is written to a separate file under scratchpad/c3fix4/scripts/; the source file is never edited.\n\nNew, KILLED (the fixture that caught it):\n- whole number-check loop removed \u2192 \"a counter that exits 1 exited 0\"\n- `\"$base_lints\"` dropped from the loop \u2192 \"a baseline row whose field 2 is not a number exited 0\"\n- `\"$base_bare\"` dropped from the loop \u2192 \"a baseline row whose field 3 is not a number exited 0\"\n- `'' |` dropped (empty accepted) \u2192 \"a counter that exits 1 exited 0\"\n- `*[!0-9]*` narrowed to `*[a-z]*` (the newline from a doubled row accepted) \u2192 \"a baseline row listed twice exited 0\"\n- loop skipped under `--update` \u2192 \"--update over a baseline row listed twice exited 0\"\n- the number check's `return 1` changed to `return 0` \u2192 \"a counter that exits 1 exited 0\"\n- :203, the outside compare always passed `update` \u2192 \"an allow outside every member over the row's first number exited 0\". The same mutation SURVIVED on the old script.\n\nNew, SURVIVED (2): `\"$2\"` alone dropped from the loop, and `\"$3\"` alone dropped. They are equivalent mutants: count() prints both numbers or neither (perl prints \"$lints $bare\\n\" or dies; the no-files path echoes \"0 0\"), so the count still in the loop is empty too and fails. The commit body says so.\n\nThe earlier 54 are all still KILLED.",
   "deviations": "Defect 1 (compare fails open on a malformed baseline row or a failed count): fixed as specified, validation loop and the exact message. I added two fixtures beyond the three asked for:\n- a second non-number fixture, so each baseline number gets `x` in turn; this kills dropping `\"$base_bare\"` from the loop, which would otherwise survive;\n- a counter-that-exits-1 fixture (`COUNTER='exit 1'`, no chmod, so it does not depend on the user not being root); it kills the whole-loop removal and the empty-value mutation.\n\nThe header's self-test paragraph, a new rule-2 sentence in the header, the README sentence and the body of dbfe3d44 (now 7931a83b) now say only what was measured: fixture list, 62 of 64 mutations killed, 2 equivalent survivors named.\n\nDefect 2 (no fixture for the outside row's first number): fixed with the `(outside-members) 0 1` fixture, which asserts a non-zero exit and both messages. The :203 mutation SURVIVED on the old script and is KILLED on the new one. Mutation count recounted in the body.\n\nNot changed: the closing MSG (\"A member stopped inheriting the workspace lints, or a row allows more than its baseline\u2026\") does not name the new failure. It already did not name the stale-row or empty-listing failures, and the \u274c line above it states the cause.",
   "review_focus": "- The COUNTER='exit 1' fixture simulates a failed perl run by replacing the counter. It does not use a real unreadable file, because chmod 000 does not stop root. The real unreadable-file case was measured by hand on the tree: rc 1 after the fix, rc 0 before. Check that the header and README wording, \"a counter that exits 1\", is not read as more than that.\n- The two surviving mutations (dropping \"$2\" or \"$3\" alone from the loop) are argued equivalent: count() prints both numbers or neither. Confirm that nothing else ever gives lints/bare only one value.\n- The new body paragraph in 7931a83b is a claim about the pre-fix behaviour: \"[ -gt ] on a value that is not a number errors, the comparison reads false, and the row passes: an unreadable .rs file and a doubled row each printed the pass line, and `--update` over the doubled row rewrote it\". It was measured with scratchpad/c3fix4/old.sh (the dbfe3d44 script) against the real tree, under bash 5 only.\n- The \u2713 line now claims \"growth in either number of either row ... refused\". The member row's second number is exercised only by the `--update` fixture (`good 15 6`), not by a plain check.\n- PR body text elsewhere may still quote \"Fifty-four mutations\"; the publisher should use 62 of 64."
  }
 ]
}