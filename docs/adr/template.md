# ADR NNNN — What the system does, as one sentence

- **Status:** Accepted
- **Date:** YYYY-MM-DD
- **Depends on:** nothing
- **Supersedes:** nothing
- **Superseded by:** nothing
- **Log:** none

<!--
Copy this file to docs/adr/NNNN-kebab-title.md, with NNNN the next unused
number, replace every placeholder and delete these comments.

"Depends on" and "Supersedes" name ADRs by relative link, as
[ADR 0001](0001-runtime-capability-tiers.md). "Log" links the ADR's
log-NNNN.md when it has one. `./scripts/check_adrs.py --check` checks these
links and an ADR's other relative links into docs/adr/. It does not check a
link out of docs/adr/, such as one into crates/.
-->

## Context

<!--
What forced a decision, and the evidence at hand. Each number names its
source, a file, a PR or the command that produced it, and the scope it was
measured over: which models, which build, how many requests.
-->

## Decision

<!--
What the system does now, in the present tense. Number the decisions when
there is more than one, so that a later ADR can cite one.
-->

## Consequences

<!--
What the decision costs, what it makes possible, and what a person using
gglib will notice.
-->

## Kill criteria

<!--
Each criterion names a reading that exists, where it is taken from, and the
value that would reverse the decision. A zero is recorded with its
denominator.
-->

## Out of scope

<!--
What this decision does not cover, so that nobody reads more into it.
-->
